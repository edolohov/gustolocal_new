# Решение проблемы зависания при сохранении хедера

## 🔍 Возможные причины:

1. **Плагин All-in-One WP Migration** - может конфликтовать с сохранением template parts
2. **Проблемы с JavaScript** - ошибки в консоли браузера блокируют сохранение
3. **Проблемы с базой данных** - блокировка таблиц или медленные запросы
4. **Проблемы с правами доступа** - WordPress не может записать в БД

## ✅ Решения:

### Решение 1: Использовать скрипт update-header.php (РЕКОМЕНДУЕТСЯ)

1. Откройте в браузере: `https://skyblue-salmon-470810.hostingersite.com/update-header.php`
2. Скрипт автоматически обновит Header из файла `parts/header.html`
3. **Удалите файл после использования!**

### Решение 2: Временно отключить All-in-One WP Migration

```bash
wp plugin deactivate all-in-one-wp-migration
```

Попробуйте сохранить хедер, затем включите плагин обратно:
```bash
wp plugin activate all-in-one-wp-migration
```

### Решение 3: Обновить через WP-CLI

Если вы редактируете файл `parts/header.html` напрямую:

```bash
ssh -i ~/.ssh/id_ed25519_hostinger -p 65002 u850527203@82.29.185.42
cd domains/gustolocal.es/public_html
HEADER_CONTENT=$(cat wp-content/themes/gustolocal/parts/header.html)
wp post update 118 --post_content="$HEADER_CONTENT"
```

### Решение 4: Проверить консоль браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Попробуйте сохранить хедер
4. Посмотрите, есть ли ошибки JavaScript

### Решение 5: Увеличить лимиты PHP

Добавьте в `wp-config.php`:

```php
@ini_set('max_execution_time', 300);
@ini_set('memory_limit', '512M');
```

## 📝 Рекомендации:

1. **Редактируйте файл напрямую** (`parts/header.html`) вместо админки
2. **Используйте скрипт** `update-header.php` для синхронизации с БД
3. **Проверьте консоль браузера** на ошибки JavaScript
4. **Временно отключите** плагин All-in-One WP Migration при редактировании

