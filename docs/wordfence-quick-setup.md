# Быстрая настройка Wordfence Security

## 🎯 Главные настройки для защиты от атак

### 1. Защита от брутфорс-атак (Rate Limiting)

**Где:** `Wordfence → Login Security → Rate Limiting`

**Настройки:**
- ✅ Включите "Enable Rate Limiting"
- **If someone's IP address hits the rate limit:** `Block the IP address`
- **How long is an IP address blocked:** `1 hour` (или больше)
- **Rate Limiting Rules:**
  - **Login failures:** `5` попыток в `15` минут
  - **Password reset requests:** `3` запроса в `60` минут (это защитит от спама писем!)
  - **404 errors:** `20` в `60` минут

**Нажмите:** "SAVE CHANGES"

---

### 2. Блокировка конкретного IP адреса

**Где:** `Wordfence → Firewall → Blocking`

**Способ 1: Быстрая блокировка (рекомендуется)**
1. Откройте `Wordfence → Firewall → Blocking`
2. В разделе "Manually Block IPs" введите: `196.251.88.101`
3. Причина: `Brute force attack - password reset spam`
4. Нажмите "Block IP"
5. ✅ IP заблокирован!

**Способ 2: Через Dashboard**
1. `Wordfence → Dashboard`
2. Найдите блок "Recently Blocked IPs"
3. Если IP там, нажмите на него и выберите "Block this IP"

---

### 3. Автоматическая блокировка подозрительных IP

**Где:** `Wordfence → Firewall → Rate Limiting`

**Настройки:**
- ✅ "Automatically block IPs that exceed these limits"
- **Block visitors who request a password reset more than:** `3` times per `60` minutes
- **Block visitors who are locked out from logging in:** `1 hour` (или больше)

---

### 4. Защита от спама писем о сбросе пароля

**Где:** `Wordfence → Login Security → Rate Limiting`

**Настройка:**
- **Password reset requests:** максимум `3` запроса в `60` минут с одного IP
- Это предотвратит отправку 20 писем за ночь!

---

## 📋 Пошаговая инструкция

### Шаг 1: Откройте Rate Limiting
1. В админке WordPress: `Wordfence → Login Security`
2. Вкладка: `Rate Limiting`
3. Нажмите "MANAGE RATE LIMITING"

### Шаг 2: Включите защиту
1. ✅ Поставьте галочку "Enable Rate Limiting"
2. Выберите: "If someone's IP address hits the rate limit" → **Block the IP address**
3. "How long is an IP address blocked" → **1 hour**

### Шаг 3: Настройте правила
В разделе "Rate Limiting Rules" установите:

```
Login failures: 5 attempts per 15 minutes
Password reset requests: 3 requests per 60 minutes
404 errors: 20 per 60 minutes
```

### Шаг 4: Сохраните
- Нажмите "SAVE CHANGES" внизу страницы

### Шаг 5: Заблокируйте IP 196.251.88.101
1. Перейдите: `Wordfence → Firewall → Blocking`
2. В разделе "Manually Block IPs" введите: `196.251.88.101`
3. Причина: `Brute force attack - password reset spam`
4. Нажмите "Block IP"

---

## ✅ Проверка

### Проверить, что IP заблокирован:
1. `Wordfence → Firewall → Blocking`
2. В разделе "Currently Blocked IPs" должен быть IP `196.251.88.101`

### Проверить работу защиты:
1. Попробуйте войти с неправильным паролем 5 раз
2. На 6-й попытке IP должен быть заблокирован
3. Проверьте: `Wordfence → Firewall → Blocking` - ваш IP должен быть в списке

---

## 🔍 Дополнительные настройки (опционально)

### Уведомления о блокировках
**Где:** `Wordfence → Tools → Diagnostics → Email Alerts`

**Настройки:**
- ✅ "Alert me when someone is locked out from logging in"
- ✅ "Alert me when someone is blocked for breaking a rate limit rule"
- Введите email для уведомлений

### Автоматическая блокировка на основе правил
**Где:** `Wordfence → Firewall → Rate Limiting`

**Настройки:**
- ✅ "Automatically block IPs that exceed these limits"
- Это автоматически заблокирует IP, которые пытаются сбросить пароль слишком часто

---

## 📝 Что делать, если придут новые атаки

### Если придет письмо о сбросе пароля:
1. Найдите IP адрес в письме (например: `IP адрес: 196.251.88.101`)
2. Откройте `Wordfence → Firewall → Blocking`
3. Введите IP в "Manually Block IPs"
4. Нажмите "Block IP"

### Если много атак за ночь:
1. Откройте `Wordfence → Dashboard`
2. Посмотрите раздел "Top IPs Blocked"
3. Заблокируйте все подозрительные IP вручную

---

## ⚠️ Важно

1. **Не блокируйте свой IP!** Если случайно заблокировали:
   - `Wordfence → Firewall → Blocking`
   - Найдите свой IP в списке
   - Нажмите "Unblock"

2. **Регулярно проверяйте Dashboard:**
   - `Wordfence → Dashboard`
   - Там будут все подозрительные активности

3. **Обновляйте Wordfence:**
   - Плагин регулярно получает обновления правил безопасности
   - `Плагины → Установленные` - проверьте, что Wordfence обновлен

---

## 🎯 Резюме: Минимальные настройки для защиты

1. ✅ **Rate Limiting:** Включить, блокировать IP на 1 час
2. ✅ **Login failures:** 5 попыток в 15 минут
3. ✅ **Password reset requests:** 3 запроса в 60 минут
4. ✅ **Заблокировать IP:** `196.251.88.101`

**Этого достаточно для защиты от текущих атак!**




