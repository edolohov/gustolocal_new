# Руководство по настройке WooCommerce и ACF для Meal Builder

## 🔧 Критические настройки WooCommerce при активации

### 1. **Валюта и формат цены** (ОБЯЗАТЕЛЬНО)
- **Валюта**: EUR (€) - **НЕ USD!**
- **Позиция валюты**: Справа от цены (right) - `12.00€`
- **Разделитель тысяч**: пробел или ничего
- **Разделитель десятичных**: запятая - `12,00€` ИЛИ точка - `12.00€` (на ваш выбор)

**Где настроить:**
`WooCommerce → Настройки → Общие → Валюта`

### 2. **Страницы WooCommerce** (ОБЯЗАТЕЛЬНО)
Убедитесь, что созданы страницы:
- **Корзина** (`/cart/`)
- **Оформление заказа** (`/checkout/`)
- **Мой аккаунт** (`/my-account/`)

Если их нет, WooCommerce предложит создать автоматически при первом запуске.

### 3. **Виртуальный продукт для Meal Builder** (ОБЯЗАТЕЛЬНО)
Нужно создать один виртуальный продукт, который будет использоваться для всех заказов из Meal Builder.

**Создание продукта:**
1. `Товары → Добавить новый`
2. **Название**: "Меню на неделю" или "Собери свое меню"
3. **Тип товара**: Простой товар
4. **Виртуальный**: ✅ Да (галочка)
5. **Загружаемый**: ❌ Нет
6. **Цена**: `0.00` (цена будет пересчитываться динамически)
7. **Опубликовать**

**Важно:** После создания продукта нужно сообщить его ID для настройки плагина.

### 4. **Настройки доставки** (ВАЖНО)
- `WooCommerce → Настройки → Доставка`
- Настройте зоны доставки (если нужны)
- Или отключите доставку, если только самовывоз

### 5. **Настройки оплаты** (ВАЖНО)
- `WooCommerce → Настройки → Платежи`
- Включите нужные методы оплаты:
  - **Наличные при доставке** (если есть)
  - **Банковский перевод** (если есть)
  - **Stripe** (если установлен плагин)

### 6. **Настройки оформления заказа** (КРИТИЧНО для ACF)
- `WooCommerce → Настройки → Оформление заказа`
- **Разрешить гостевые заказы**: ✅ Да (если не требуете регистрацию)
- **Включить купоны**: по желанию

---

## 🎯 Настройка Advanced Custom Fields (ACF) для упрощения формы

### Цель
Минимизировать поля в форме оформления заказа, оставив только самое необходимое.

### План действий:

#### 1. **Активировать ACF**
- `Плагины → Установленные плагины → Advanced Custom Fields → Активировать`

#### 2. **Создать группу полей для заказов**
1. `Настраиваемые поля → Добавить новую группу`
2. **Название группы**: "Поля оформления заказа"
3. **Расположение**: 
   - `Правила показа` → `Правило 1`:
     - `Параметр`: `Страница`
     - `Оператор`: `Равно`
     - `Значение`: `Оформление заказа` (выберите страницу checkout)

#### 3. **Какие поля оставить/убрать**

**ОБЯЗАТЕЛЬНЫЕ поля (оставить):**
- ✅ Имя
- ✅ Фамилия  
- ✅ Email
- ✅ Телефон
- ✅ Адрес доставки (если доставка)
- ✅ Комментарий к заказу (опционально)

**УБРАТЬ (если не нужны):**
- ❌ Компания
- ❌ Страна (если только Испания)
- ❌ Регион/Область (если не нужен)
- ❌ Почтовый индекс (если не критично)
- ❌ Дополнительные поля адреса

#### 4. **Настройка через код (рекомендуется)**

Вместо ACF можно использовать хуки WooCommerce для удаления/изменения полей:

```php
// Убрать ненужные поля
add_filter('woocommerce_checkout_fields', function($fields) {
    // Убрать компанию
    unset($fields['billing']['billing_company']);
    
    // Убрать страну (если только Испания)
    // unset($fields['billing']['billing_country']);
    
    // Сделать почтовый индекс необязательным
    $fields['billing']['billing_postcode']['required'] = false;
    
    return $fields;
});
```

---

## 🔗 Интеграция Meal Builder с WooCommerce

### Текущая интеграция (уже работает):
1. ✅ Meal Builder добавляет товары в корзину через AJAX
2. ✅ Цена пересчитывается динамически на основе выбранных блюд
3. ✅ Детали заказа (список блюд) сохраняются в метаданных заказа
4. ✅ Отображаются в корзине и в заказе

### Что нужно проверить:
1. **Product ID в плагине**: Проверьте, что `WMB_PRODUCT_ID` в `weekly-meal-builder.php` соответствует ID созданного виртуального продукта
2. **Страница корзины**: Убедитесь, что `/cart/` существует и доступна
3. **Страница оформления**: Убедитесь, что `/checkout/` существует

---

## 📋 Чек-лист после настройки

- [ ] Валюта установлена на EUR (€)
- [ ] Формат цены правильный (12,00€ или 12.00€)
- [ ] Создан виртуальный продукт для Meal Builder
- [ ] Product ID обновлен в плагине (если отличается от 299)
- [ ] Страницы корзины и оформления созданы
- [ ] Настроены методы оплаты
- [ ] Настроены зоны доставки (если нужны)
- [ ] ACF активирован
- [ ] Лишние поля убраны из формы оформления
- [ ] Протестирован полный цикл: выбор блюд → корзина → оформление → заказ

---

## 🚨 Частые проблемы

### Проблема: "WooCommerce не активирован"
**Решение**: Убедитесь, что WooCommerce активен (`Плагины → Установленные плагины`)

### Проблема: "Не удалось добавить в корзину"
**Решение**: 
1. Проверьте, что Product ID правильный
2. Проверьте, что продукт опубликован
3. Проверьте права доступа к корзине

### Проблема: Цена не пересчитывается
**Решение**: Проверьте, что в корзине правильно применяется фильтр `woocommerce_before_calculate_totals`

---

## 📞 Следующие шаги

После настройки WooCommerce и ACF:
1. Протестируйте добавление товаров в корзину из Meal Builder
2. Проверьте отображение деталей заказа в корзине
3. Протестируйте оформление заказа
4. Проверьте, что заказ сохраняется с правильными данными

