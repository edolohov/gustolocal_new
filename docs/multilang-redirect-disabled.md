# Отключение автоматического редиректа на /es/ и /en/

## ✅ Что было сделано

Автоматический редирект на `/es/` и `/en/` временно отключен, чтобы пользователи не попадали на 404 ошибку.

### Изменения в `functions.php`:

1. **JavaScript автоматический редирект (строки 541-574):**
   - ❌ Отключен редирект при первом визите на основе языка браузера
   - ✅ Язык браузера сохраняется в localStorage/cookie для будущего использования
   - ✅ Пользователи видят русскую версию по умолчанию

2. **JavaScript ручное переключение (строки 576-594):**
   - ❌ Отключен редирект на `/es/` и `/en/` при клике на переключатель языка
   - ✅ Редирект на русский (`/`) работает
   - ✅ Выбор языка сохраняется в cookie/localStorage

3. **PHP редирект по cookie (строки 648-675):**
   - ❌ Отключен редирект на `/es/` и `/en/` на основе cookie
   - ✅ Редирект на русский работает (если пользователь выбрал русский)

## 📊 Текущее поведение

| Ситуация | Что происходит |
|----------|----------------|
| Первый визит, испанский браузер | ✅ Видит русскую версию, язык сохраняется в cookie |
| Первый визит, английский браузер | ✅ Видит русскую версию, язык сохраняется в cookie |
| Первый визит, русский браузер | ✅ Видит русскую версию |
| Клик на переключатель ES/EN | ✅ Выбор сохраняется, но редиректа нет (остается на русской версии) |
| Клик на переключатель RU | ✅ Редирект на `/` (русская версия) |
| Повторный визит с cookie es/en | ✅ Видит русскую версию (редирект отключен) |

## 🔄 Как включить обратно

Когда будут созданы страницы `-es` и `-en`:

1. **Включить JavaScript автоматический редирект:**
   - Найти строку `// ВРЕМЕННО ОТКЛЮЧЕНО: Автоматический редирект отключен`
   - Раскомментировать блок кода (убрать комментарии)
   - Удалить строки с `// ВРЕМЕННО ОТКЛЮЧЕНО`

2. **Включить JavaScript ручное переключение:**
   - Найти строку `// ВРЕМЕННО: Редирект только на русский`
   - Убрать проверку `if (targetLang === 'ru')` и оставить весь блок

3. **Включить PHP редирект по cookie:**
   - Найти строку `// ВРЕМЕННО: Редирект только на русский`
   - Убрать проверку `if ($saved_lang === 'ru')` и оставить весь блок

## 📝 Примечания

- ✅ Переводы интерфейса WooCommerce работают через `translations.php`
- ✅ Пользователи могут использовать встроенный перевод браузера
- ✅ Язык браузера сохраняется для будущего использования
- ✅ Все изменения помечены комментариями `// ВРЕМЕННО ОТКЛЮЧЕНО` для легкого поиска

## 🎯 Следующие шаги

1. Создать страницы с суффиксами `-es` и `-en` для всех важных страниц
2. Протестировать на STG
3. Включить редиректы обратно
4. Протестировать автоматическое определение языка

