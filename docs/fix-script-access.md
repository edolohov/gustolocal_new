# 🔧 Исправление доступа к скрипту find-backdoor-script.php

## Проблема
Ошибка: "Forbidden - You don't have permission to access /find-backdoor-script.php"

**Причина:** Файл `.htaccess` блокирует все PHP файлы, кроме тех, что в белом списке.

---

## ✅ РЕШЕНИЕ 1: Загрузите скрипт в папку wp-content/uploads/ (РЕКОМЕНДУЕТСЯ)

Эта папка обычно не блокируется `.htaccess`.

### Шаг 1: Загрузите скрипт

1. Скопируйте `docs/find-backdoor-script.php` в папку:
   ```
   /home/s1149026/gustolocal.es/wp-content/uploads/find-backdoor-script.php
   ```

2. Откройте в браузере:
   ```
   https://gustolocal.es/wp-content/uploads/find-backdoor-script.php?key=CHANGE_THIS_PASSWORD
   ```

3. **ВАЖНО:** После использования удалите файл!

---

## ✅ РЕШЕНИЕ 2: Добавьте файл в белый список .htaccess

Если хотите запустить скрипт из корня сайта:

### Шаг 1: Найдите корневой .htaccess

Файл должен быть в корне WordPress:
```
/home/s1149026/gustolocal.es/.htaccess
```

### Шаг 2: Добавьте find-backdoor-script.php в белый список

Найдите строку:
```apache
<FilesMatch '^(index.php|wp-blog-header.php|...)$'>
```

Добавьте `find-backdoor-script.php` в список:
```apache
<FilesMatch '^(index.php|wp-blog-header.php|...|find-backdoor-script.php)$'>
```

**Или добавьте отдельное правило перед блокировкой:**
```apache
<FilesMatch '^find-backdoor-script.php$'>
Order allow,deny
Allow from all
</FilesMatch>
```

### Шаг 3: Сохраните .htaccess

### Шаг 4: Откройте скрипт

```
https://gustolocal.es/find-backdoor-script.php?key=CHANGE_THIS_PASSWORD
```

---

## ✅ РЕШЕНИЕ 3: Переименуйте скрипт (БЫСТРОЕ РЕШЕНИЕ)

Переименуйте файл в имя из белого списка `.htaccess`:

### Вариант A: Используйте admin-ajax.php

1. Загрузите скрипт как:
   ```
   /home/s1149026/gustolocal.es/admin-ajax.php
   ```
   (Временно, если файл не существует)

2. Откройте:
   ```
   https://gustolocal.es/admin-ajax.php?key=CHANGE_THIS_PASSWORD
   ```

3. **ВАЖНО:** После использования удалите файл!

### Вариант B: Используйте wp-load.php (если не используется)

1. Сделайте бэкап оригинального `wp-load.php` (если существует)
2. Загрузите скрипт как:
   ```
   /home/s1149026/gustolocal.es/wp-load.php
   ```
3. Откройте:
   ```
   https://gustolocal.es/wp-load.php?key=CHANGE_THIS_PASSWORD
   ```
4. После использования восстановите оригинальный `wp-load.php`

---

## 🔑 Про пароль в URL

**Это НЕ пароль от админки WordPress или базы данных!**

Это ключ доступа, который указан в самом скрипте.

### По умолчанию пароль:
```
CHANGE_THIS_PASSWORD
```

### Как использовать:
```
https://gustolocal.es/find-backdoor-script.php?key=CHANGE_THIS_PASSWORD
```

### Если хотите изменить пароль:

1. Откройте файл `find-backdoor-script.php` в текстовом редакторе
2. Найдите строку:
   ```php
   $ACCESS_KEY = 'CHANGE_THIS_PASSWORD';
   ```
3. Замените на свой пароль:
   ```php
   $ACCESS_KEY = 'ВАШ_СЕКРЕТНЫЙ_ПАРОЛЬ';
   ```
4. Используйте новый пароль в URL:
   ```
   https://gustolocal.es/find-backdoor-script.php?key=ВАШ_СЕКРЕТНЫЙ_ПАРОЛЬ
   ```

---

## 📋 ЧЕК-ЛИСТ

- [ ] Выбрано решение (рекомендуется: загрузить в wp-content/uploads/)
- [ ] Скрипт загружен в правильную папку
- [ ] Открыт URL с правильным паролем (?key=CHANGE_THIS_PASSWORD)
- [ ] Скрипт работает и показывает результаты
- [ ] Сохранены результаты сканирования
- [ ] Скрипт удален после использования

---

## ⚠️ ВАЖНО

1. **Удалите скрипт после использования** - это важно для безопасности!
2. **Не используйте простые пароли** - если меняете пароль, используйте сложный
3. **Не оставляйте скрипт на сервере** - удалите сразу после использования
4. **Если используете Решение 2** - удалите правило из `.htaccess` после использования скрипта

---

## 🎯 РЕКОМЕНДАЦИЯ

**Используйте Решение 1** (загрузить в `wp-content/uploads/`) - это самое простое и безопасное решение, не требует изменения `.htaccess`.




