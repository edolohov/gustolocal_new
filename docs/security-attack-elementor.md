# Критическая ситуация: Атака на сайт через Elementor

## 🔴 Что происходит

Ваш сайт подвергается **массовой атаке** через уязвимость в плагине Elementor. Это **НЕ проблема хостинга**, а проблема безопасности вашего сайта.

### Анализ логов

Из ответа поддержки видно:
- **Множество запросов** к одному и тому же файлу:
  ```
  /wp-content/plugins/elementor/assets/lib/animations/animations/index.php?ARRAY=7o2230223n3
  ```
- Все процессы показывают статус `[B]` (BUSY) - заняты обработкой атакующих запросов
- Обработчики веб-сервера перегружены
- Новые запросы получают ошибку **502** (Bad Gateway)

### Что это значит

1. **Это атака/эксплойт** - злоумышленники пытаются использовать уязвимость в Elementor
2. **Проблема на стороне сайта**, а не хостинга
3. **Сайт перегружен** атакующими запросами
4. **Нужна срочная защита**

---

## ✅ Что делать СРОЧНО

### Шаг 1: Заблокируйте атакующие запросы через .htaccess

Добавьте в `.htaccess` в корне WordPress (в самое начало, перед другими правилами):

```apache
# БЛОКИРОВКА АТАКИ НА ELEMENTOR
# Блокируем подозрительные запросы к animations/index.php
<IfModule mod_rewrite.c>
RewriteEngine On

# Блокировка запросов с параметром ARRAY к animations
RewriteCond %{QUERY_STRING} ARRAY= [NC]
RewriteCond %{REQUEST_URI} animations/index\.php [NC]
RewriteRule .* - [F,L]

# Блокировка всех запросов к animations/index.php (если не нужен)
RewriteCond %{REQUEST_URI} /wp-content/plugins/elementor/assets/lib/animations/animations/index\.php [NC]
RewriteRule .* - [F,L]
</IfModule>

# Блокировка по User-Agent (если это боты)
<IfModule mod_rewrite.c>
RewriteCond %{HTTP_USER_AGENT} ^$ [OR]
RewriteCond %{HTTP_USER_AGENT} (bot|crawler|spider|scraper) [NC]
RewriteCond %{REQUEST_URI} elementor [NC]
RewriteRule .* - [F,L]
</IfModule>
```

### Шаг 2: Обновите Elementor до последней версии

1. Войдите в админку WordPress (если получится)
2. Перейдите в **Плагины** → **Установленные плагины**
3. Найдите **Elementor**
4. Нажмите **Обновить** (если доступно)

**Или через SFTP:**
1. Удалите старую версию: `/wp-content/plugins/elementor/`
2. Установите последнюю версию через админку

### Шаг 3: Временно отключите Elementor

Если сайт все еще недоступен:

**Через SFTP:**
1. Переименуйте папку: `/wp-content/plugins/elementor/` → `/wp-content/plugins/elementor-disabled/`
2. Сайт должен заработать (но без Elementor)

**Через базу данных:**
```sql
-- Отключить Elementor через БД
UPDATE `dp___bk_250951_wp_options` 
SET `option_value` = REPLACE(`option_value`, 'elementor/elementor.php', '') 
WHERE `option_name` = 'active_plugins';
```

### Шаг 4: Установите/настройте плагин безопасности

**Рекомендуемые плагины:**
- **Wordfence Security** (бесплатный, очень эффективный)
- **Sucuri Security**
- **iThemes Security**

**Быстрая установка Wordfence:**
1. Скачайте через админку WordPress
2. Активируйте
3. Запустите сканирование
4. Включите **Rate Limiting** (ограничение запросов)
5. Включите **Firewall**

### Шаг 5: Заблокируйте IP адреса атакующих

**Через .htaccess:**
```apache
# Блокировка конкретных IP (если знаете адреса атакующих)
<Limit GET POST>
    Order allow,deny
    Deny from 141.8.192.20  # Пример IP
    Allow from all
</Limit>
```

**Через Wordfence:**
- Перейдите в **Wordfence** → **Firewall** → **Blocking**
- Добавьте IP адреса вручную

---

## 🔍 Дополнительная диагностика

### Проверьте файл animations/index.php

**Через SFTP:**
1. Перейдите в `/wp-content/plugins/elementor/assets/lib/animations/animations/`
2. Проверьте файл `index.php`
3. Если он содержит подозрительный код - это может быть взлом

**Нормальный файл должен быть пустым или содержать только:**
```php
<?php
// Silence is golden.
```

### Проверьте другие подозрительные файлы

Ищите файлы с недавними датами изменения:
```bash
# Через SSH (если есть доступ)
find /home/s1149026/gustolocal.es/wp-content/plugins/elementor -type f -mtime -1
```

### Проверьте процесс python bot_personal.py

В логах виден процесс:
```
python bot_personal.py - работает 44 минуты
```

**Проверьте:**
1. Это ваш легитимный скрипт?
2. Если нет - это может быть бэкдор/бот
3. Найдите файл и проверьте его содержимое

---

## 🛡️ Долгосрочная защита

### 1. Обновляйте все плагины регулярно

Устаревшие плагины - главная причина уязвимостей.

### 2. Используйте плагин безопасности

- **Wordfence** - лучший выбор для WordPress
- Настройте автоматическое сканирование
- Включите Firewall

### 3. Ограничьте доступ к wp-admin

**Через .htaccess:**
```apache
# Ограничение доступа к wp-admin по IP (опционально)
<Files wp-login.php>
    Order deny,allow
    Deny from all
    Allow from YOUR_IP_ADDRESS  # Замените на ваш IP
</Files>
```

### 4. Используйте Cloudflare

- Бесплатный план Cloudflare защищает от DDoS
- Автоматически блокирует ботов
- Кеширует контент

### 5. Регулярные бэкапы

- Делайте бэкапы перед обновлениями
- Храните бэкапы отдельно от сервера

---

## 📊 Мониторинг атак

### Настройте уведомления

**В Wordfence:**
- **Wordfence** → **All Options** → **Email Alert Preferences**
- Включите уведомления о блокировках

### Проверяйте логи регулярно

- **Wordfence** → **Tools** → **Live Traffic**
- Смотрите подозрительную активность

---

## ❓ Ответы на вопросы

### Нужно ли менять хостинг?

**НЕТ, не нужно.** Проблема не в хостинге, а в безопасности сайта. Хостинг просто показывает, что сервер перегружен атакой.

### На чьей стороне проблема?

**На стороне сайта** - уязвимость в плагине Elementor или взломанный файл.

### Что делать, если сайт все еще недоступен?

1. Отключите Elementor через SFTP (переименуйте папку)
2. Добавьте правила блокировки в .htaccess
3. Дождитесь, пока атака прекратится
4. Обновите Elementor
5. Включите обратно

---

## 🆘 План действий (по приоритету)

1. ✅ **СРОЧНО:** Добавьте правила блокировки в `.htaccess`
2. ✅ **СРОЧНО:** Отключите Elementor временно (если сайт не работает)
3. ✅ **ВАЖНО:** Обновите Elementor до последней версии
4. ✅ **ВАЖНО:** Установите Wordfence Security
5. ✅ **РЕКОМЕНДУЕТСЯ:** Настройте Cloudflare
6. ✅ **РЕГУЛЯРНО:** Обновляйте все плагины и WordPress

---

## 📝 Шаблон .htaccess для защиты

Создайте файл `.htaccess` в корне WordPress с таким содержимым:

```apache
# BEGIN WordPress Security
<IfModule mod_rewrite.c>
RewriteEngine On

# Блокировка атаки на Elementor animations
RewriteCond %{QUERY_STRING} ARRAY= [NC]
RewriteCond %{REQUEST_URI} animations/index\.php [NC]
RewriteRule .* - [F,L]

# Блокировка всех запросов к animations/index.php
RewriteCond %{REQUEST_URI} /wp-content/plugins/elementor/assets/lib/animations/animations/index\.php [NC]
RewriteRule .* - [F,L]

# Блокировка подозрительных User-Agent
RewriteCond %{HTTP_USER_AGENT} ^$ [OR]
RewriteCond %{HTTP_USER_AGENT} (bot|crawler|spider|scraper|wget|curl) [NC]
RewriteCond %{REQUEST_URI} (elementor|wp-admin|wp-login) [NC]
RewriteRule .* - [F,L]
</IfModule>

# Ограничение размера запроса
LimitRequestBody 10485760

# Защита от hotlinking
<IfModule mod_rewrite.c>
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^https?://(www\.)?gustolocal\.es [NC]
RewriteCond %{REQUEST_URI} \.(jpg|jpeg|png|gif|webp)$ [NC]
RewriteRule .* - [F]
</IfModule>
# END WordPress Security

# BEGIN WordPress
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /
RewriteRule ^index\.php$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.php [L]
</IfModule>
# END WordPress
```

---

## ⚠️ Важно

1. **Не паникуйте** - это решаемо
2. **Действуйте быстро** - добавьте правила блокировки
3. **Обновите Elementor** - это критично
4. **Установите защиту** - Wordfence обязателен
5. **Мониторьте** - следите за логами

---

## 📞 Если ничего не помогает

1. Обратитесь в поддержку хостинга с просьбой временно заблокировать IP атакующих
2. Попросите их помочь настроить mod_security правила
3. Рассмотрите переход на Cloudflare для защиты от DDoS




