# Аудит мультиязычности: Текущее состояние и план

## 📋 Текущее состояние (PROD)

### ✅ Что РАБОТАЕТ (активно):

1. **Файл переводов** (`translations.php`)
   - ✅ Подключен в `functions.php` (строка 12)
   - ✅ Содержит переводы для RU, ES, EN
   - ✅ Функции `get_translations()` и `get_translation()` работают

2. **Система переводов WooCommerce** (в `functions.php`, строки 676-837)
   - ✅ Переводы строк WooCommerce через фильтры
   - ✅ Переводы полей формы чекаута
   - ✅ Переводы сообщений корзины
   - ✅ Переводы кнопок и заголовков
   - ✅ Переводы страниц 404

3. **Базовая маршрутизация** (в `functions.php`, строки 227-344)
   - ✅ Rewrite rules для `/es/` и `/en/`
   - ✅ Функция `get_current_language()` - определяет язык из URL
   - ✅ Функция `simple_page_lookup()` - ищет страницы с суффиксами `-es` и `-en`
   - ✅ Специальная обработка WooCommerce страниц (cart, checkout, my-account)

4. **Простое перенаправление языка** (`simple_language_redirect`, строка 647)
   - ✅ Редирект на основе cookie `user_language`
   - ⚠️ Работает только если cookie уже установлен

### ⚠️ Что НЕ ПОДКЛЮЧЕНО (но файлы есть):

1. **`multilang-core.php`**
   - ❌ Файл существует, но НЕ подключен в `functions.php`
   - ❌ Содержит дублирующий код маршрутизации
   - ❌ Содержит отладочные функции (debug в footer)
   - ⚠️ Похоже, что код из него был скопирован в `functions.php` и файл отключен

2. **`multilang-seo-functions.php`**
   - ❌ Файл был создан, но **ОТКАТНУТ** (не должен быть на PROD)
   - ❌ Не подключен в `functions.php`
   - ⚠️ Содержит hreflang теги, автоопределение языка
   - ⚠️ Может конфликтовать с `simple_language_redirect`
   - ✅ **Ошибка исправлена**: переменная `$accept_language` определена правильно

### ❌ Что НЕ РАБОТАЕТ:

1. **Автоматическое определение языка при первом визите**
   - ❌ Нет проверки `Accept-Language` заголовка
   - ❌ Нет редиректа на язык браузера

2. **Hreflang теги для SEO**
   - ❌ Не добавляются в `<head>` (если `multilang-seo-functions.php` не работает)

3. **Canonical URLs**
   - ❌ Не настроены для каждой языковой версии

4. **Языковые атрибуты HTML**
   - ❌ Не добавляется `lang="es"` или `lang="en"` в `<html>` тег

## 📋 Текущее состояние (STG)

### ✅ Что РАБОТАЕТ (активно):

1. **Файл переводов** (`translations.php`)
   - ✅ Подключен в `functions.php` (строка 12)
   - ✅ Идентичен PROD версии

2. **Система переводов WooCommerce** (в `functions.php`, строки 673-836)
   - ✅ Идентична PROD версии

3. **Базовая маршрутизация** (в `functions.php`, строки 227-341)
   - ✅ Идентична PROD версии

4. **Простое перенаправление языка** (`simple_language_redirect`, строка 644)
   - ✅ Идентично PROD версии

### ❌ Что ОТСУТСТВУЕТ:

1. **`multilang-seo-functions.php`**
   - ❌ Файл отсутствует в STG
   - ❌ Нет hreflang тегов
   - ❌ Нет автоопределения языка

2. **`multilang-core.php`**
   - ⚠️ Файл существует, но НЕ подключен (как и в PROD)

## 🔍 Проблемы и несоответствия

### 1. Дублирование кода
- Код маршрутизации дублируется в `functions.php` и `multilang-core.php`
- `multilang-core.php` не используется, но код из него скопирован в `functions.php`

### 2. Конфликт редиректов
- `simple_language_redirect()` (строка 647) работает на основе cookie
- `auto_detect_language_redirect()` (в `multilang-seo-functions.php`) тоже работает на основе cookie + Accept-Language
- ⚠️ Оба могут срабатывать одновременно и конфликтовать

### 3. Ошибка в `multilang-seo-functions.php`
```php
// Строка 201 - ОШИБКА:
$detected_lang = detect_language_from_header($accept_language);
// Переменная $accept_language не определена!
// Должно быть: $_SERVER['HTTP_ACCEPT_LANGUAGE']
```

### 4. Неполная реализация
- Маршрутизация работает только для страниц с суффиксами `-es` и `-en`
- Нет обработки товаров WooCommerce
- Нет обработки категорий товаров

## 📝 План (что было задумано)

### Исходный план (судя по коду):

1. **Базовая маршрутизация** ✅ (реализовано)
   - URL префиксы `/es/` и `/en/`
   - Поиск страниц с суффиксами `-es` и `-en`

2. **Переводы WooCommerce** ✅ (реализовано)
   - Словарь переводов в `translations.php`
   - Фильтры для перевода строк

3. **Автоматическое определение языка** ⚠️ (частично)
   - Cookie-based редирект ✅
   - Accept-Language редирект ❌ (есть код, но с ошибкой)

4. **SEO оптимизация** ❌ (не реализовано)
   - Hreflang теги (есть код, но с ошибкой)
   - Canonical URLs (есть код, но не протестирован)
   - HTML lang атрибуты (есть код, но не протестирован)

5. **Перевод кастомных билдеров** ✅ (реализовано)
   - `meal-builder-multilang.php` - переводы для Weekly Meal Builder

## 🎯 Рекомендации для STG тестирования

### Шаг 1: Исправить ошибки
1. Исправить ошибку в `multilang-seo-functions.php` (строка 201)
2. Убрать конфликт между `simple_language_redirect` и `auto_detect_language_redirect`

### Шаг 2: Очистить дублирование
1. Решить: использовать `multilang-core.php` или код в `functions.php`?
2. Если использовать `multilang-core.php` - подключить его и удалить дубликат из `functions.php`
3. Если оставить в `functions.php` - удалить `multilang-core.php`

### Шаг 3: Протестировать на STG
1. Скопировать исправленный `multilang-seo-functions.php` в STG
2. Подключить его в `functions.php`
3. Протестировать:
   - Работу маршрутизации `/es/` и `/en/`
   - Автоопределение языка
   - Hreflang теги
   - Переводы WooCommerce
   - Работу корзины и чекаута на разных языках

### Шаг 4: Оптимизация
1. Убрать отладочный код (debug в footer)
2. Оптимизировать производительность
3. Добавить обработку товаров и категорий WooCommerce

## ⚠️ ВАЖНО: Что НЕ делать на PROD

- ❌ НЕ исправлять ошибки на PROD
- ❌ НЕ добавлять новые функции на PROD
- ❌ НЕ менять существующий код на PROD
- ✅ ВСЁ тестировать сначала на STG

**✅ ИЗМЕНЕНИЯ ОТКАТНУТЫ**: Файл `multilang-seo-functions.php` удален с PROD, подключение убрано из `functions.php`

## 📊 Сводная таблица

| Функция | PROD | STG | Статус |
|---------|------|-----|--------|
| Переводы (`translations.php`) | ✅ | ✅ | Работает |
| Маршрутизация `/es/`, `/en/` | ✅ | ✅ | Работает |
| Переводы WooCommerce | ✅ | ✅ | Работает |
| Cookie-based редирект | ✅ | ✅ | Работает |
| Accept-Language редирект | ❌ | ❌ | Не работает (ошибка) |
| Hreflang теги | ❌ | ❌ | Не работает (ошибка) |
| Canonical URLs | ❌ | ❌ | Не протестировано |
| HTML lang атрибуты | ❌ | ❌ | Не протестировано |
| Перевод билдеров | ✅ | ✅ | Работает |

## 🔧 Следующие шаги

1. **Создать STG окружение** (если еще не готово)
2. **Исправить ошибки** в `multilang-seo-functions.php`
3. **Убрать конфликты** между редиректами
4. **Протестировать на STG**
5. **Документировать результаты**
6. **Развернуть на PROD** только после успешного тестирования

