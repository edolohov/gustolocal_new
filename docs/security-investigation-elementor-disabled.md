# Расследование: Атака продолжается, хотя Elementor отключен

## 🔍 Ситуация

- Wordfence установлен
- Elementor отключен
- Но атака все еще идет на файлы Elementor

Это означает, что проблема **глубже**, чем просто активный плагин.

---

## 🚨 Возможные причины

### 1. Файлы Elementor все еще на сервере (даже если отключены)

**Проблема:** Отключение плагина через админку НЕ удаляет файлы. Они остаются на сервере и могут быть взломанными.

**Решение:** Полностью удалите папку Elementor через SFTP.

### 2. Взломанные файлы Elementor содержат бэкдор

**Проблема:** Файл `animations/index.php` может быть взломан и содержать вредоносный код, который выполняется даже если плагин отключен.

**Решение:** Проверьте содержимое файла и удалите, если подозрительный.

### 3. Wordfence не блокирует эти запросы

**Проблема:** Wordfence может быть не настроен правильно или не обновлен.

**Решение:** Проверьте настройки Wordfence и обновите правила.

### 4. Бэкдор в другом месте

**Проблема:** Взломанный файл может быть в другом месте, но использует путь к Elementor для маскировки.

**Решение:** Полное сканирование на наличие бэкдоров.

---

## ✅ ПЛАН ДЕЙСТВИЙ

### Шаг 1: Проверьте, действительно ли Elementor отключен

**Через базу данных (phpMyAdmin):**
```sql
-- Проверьте активные плагины
SELECT option_value 
FROM `dp___bk_250951_wp_options` 
WHERE option_name = 'active_plugins';
```

Если в результате есть `elementor/elementor.php` - плагин все еще активен!

**Через SFTP:**
1. Проверьте папку: `/wp-content/plugins/elementor/`
2. Если папка существует - плагин НЕ удален, только отключен

### Шаг 2: ПОЛНОСТЬЮ удалите Elementor

**Через SFTP:**
1. Подключитесь: `sftp://s1149026@141.8.192.20`
2. Перейдите в: `/home/s1149026/gustolocal.es/wp-content/plugins/`
3. **УДАЛИТЕ** папку `elementor` (не переименовывайте, именно удалите!)
4. Если есть папка `elementor-pro` - удалите и её

**ВАЖНО:** Удалите полностью, не просто переименовывайте!

### Шаг 3: Проверьте файл animations/index.php (если еще существует)

**Через SFTP:**
1. Перейдите в: `/wp-content/plugins/elementor/assets/lib/animations/animations/`
2. Откройте файл `index.php`
3. **Он должен содержать ТОЛЬКО:**
   ```php
   <?php
   // Silence is golden.
   ```
4. Если там другой код - файл взломан!

**Подозрительные признаки:**
- Код `eval()`, `base64_decode()`, `exec()`, `system()`
- Длинные строки base64
- Подозрительные функции PHP
- Любой код кроме "Silence is golden"

### Шаг 4: Проверьте настройки Wordfence

**В админке WordPress:**
1. Перейдите в **Wordfence** → **All Options**
2. Проверьте:
   - **Firewall** → **Enabled** (должно быть включено)
   - **Rate Limiting** → **Enabled** (должно быть включено)
   - **Brute Force Protection** → **Enabled** (должно быть включено)

3. Проверьте **Firewall Rules**:
   - **Wordfence** → **Firewall** → **Rules**
   - Убедитесь, что правила обновлены

4. Запустите **сканирование**:
   - **Wordfence** → **Scan** → **Start New Scan**

### Шаг 5: Добавьте правила блокировки в .htaccess

Даже если Elementor удален, добавьте правила на случай, если файлы остались:

```apache
# БЛОКИРОВКА АТАКИ НА ELEMENTOR (даже если плагин удален)
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /

# Блокировка всех запросов к animations/index.php
RewriteCond %{REQUEST_URI} /wp-content/plugins/elementor/assets/lib/animations/animations/index\.php [NC]
RewriteRule .* - [F,L]

# Блокировка запросов с параметром ARRAY
RewriteCond %{QUERY_STRING} ARRAY= [NC]
RewriteRule .* - [F,L]

# Блокировка всех запросов к папке elementor (если плагин удален)
RewriteCond %{REQUEST_URI} /wp-content/plugins/elementor [NC]
RewriteRule .* - [F,L]
</IfModule>
```

### Шаг 6: Проверьте на наличие бэкдоров

**Что искать:**

1. **Подозрительные файлы в корне:**
   - `wp-config-backup.php`
   - `wp-admin.php`
   - `wp-login-backup.php`
   - Любые `.php` файлы с недавними датами

2. **Подозрительные файлы в wp-content:**
   - `/wp-content/uploads/` - файлы с расширением `.php`
   - `/wp-content/themes/` - подозрительные файлы в темах
   - `/wp-content/plugins/` - неизвестные плагины

3. **Проверьте процесс python bot_personal.py:**
   - Найдите файл через SFTP
   - Проверьте его содержимое
   - Если это не ваш скрипт - удалите его

**Через Wordfence:**
- Запустите полное сканирование
- Проверьте результаты на наличие бэкдоров

### Шаг 7: Проверьте логи Wordfence

**В админке WordPress:**
1. **Wordfence** → **Tools** → **Live Traffic**
2. Посмотрите, блокирует ли Wordfence эти запросы
3. Если нет - проверьте настройки Firewall

**Что искать:**
- Запросы к `animations/index.php`
- IP адреса, которые делают эти запросы
- Статус блокировки (Blocked/Allowed)

---

## 🔍 Дополнительная диагностика

### Проверьте, есть ли другие сайты с Elementor

В логах видно, что атака идет на `mirecuerdo.es` (другой ваш сайт?). Проверьте:

1. Есть ли Elementor на других сайтах?
2. Если да - удалите его везде
3. Добавьте правила блокировки на всех сайтах

### Проверьте кеш

Если используется кеширование:
1. Очистите весь кеш
2. Проверьте, нет ли кешированных запросов к Elementor

### Проверьте базу данных

**Через phpMyAdmin:**
```sql
-- Поиск упоминаний Elementor в базе данных
SELECT * FROM `dp___bk_250951_wp_options` 
WHERE option_value LIKE '%elementor%';

-- Поиск в постах
SELECT * FROM `dp___bk_250951_wp_posts` 
WHERE post_content LIKE '%elementor%';
```

---

## 🛡️ Долгосрочные меры

### 1. Обновите Wordfence

1. **Wordfence** → **Dashboard**
2. Проверьте, есть ли обновления
3. Обновите до последней версии

### 2. Настройте Wordfence правильно

**Обязательные настройки:**
- **Firewall** → **Enabled** + **Learning Mode** → **Enabled** (на 7 дней, потом переключите на **Enabled and Protecting**)
- **Rate Limiting** → **Enabled**
- **Brute Force Protection** → **Enabled**
- **2FA** (двухфакторная аутентификация) для администраторов

### 3. Регулярные сканирования

Настройте автоматические сканирования:
- **Wordfence** → **All Options** → **Scan Schedule**
- Выберите ежедневное сканирование

### 4. Мониторинг

Регулярно проверяйте:
- **Wordfence** → **Tools** → **Live Traffic**
- **Wordfence** → **Scan** → **Results**

---

## 📋 Чек-лист расследования

- [ ] Проверено, что Elementor действительно отключен в БД
- [ ] Полностью удалена папка Elementor через SFTP
- [ ] Проверен файл `animations/index.php` на наличие бэкдора
- [ ] Проверены настройки Wordfence (Firewall, Rate Limiting)
- [ ] Запущено полное сканирование Wordfence
- [ ] Добавлены правила блокировки в .htaccess
- [ ] Проверены логи Wordfence на блокировки
- [ ] Проверены другие сайты на наличие Elementor
- [ ] Проверен процесс `python bot_personal.py`
- [ ] Проверена база данных на упоминания Elementor

---

## 🆘 Если проблема сохраняется

### 1. Обратитесь в поддержку хостинга

Попросите их:
- Заблокировать IP адреса атакующих на уровне сервера
- Проверить логи Apache на наличие подозрительной активности
- Помочь найти источник атаки

### 2. Проведите полную проверку безопасности

- Используйте **Wordfence** для полного сканирования
- Проверьте все файлы на наличие бэкдоров
- Проверьте базу данных на наличие вредоносного кода

### 3. Рассмотрите восстановление из бэкапа

Если найдены бэкдоры:
- Восстановите сайт из чистого бэкапа (до взлома)
- Убедитесь, что бэкап не содержит вредоносный код
- После восстановления сразу установите все обновления

---

## ⚠️ ВАЖНО

1. **Удалите Elementor полностью** - не просто отключайте
2. **Проверьте файлы на бэкдоры** - взломанные файлы могут работать даже если плагин отключен
3. **Настройте Wordfence правильно** - он должен блокировать эти запросы
4. **Добавьте правила в .htaccess** - дополнительная защита на уровне сервера

---

## 📝 Вопросы для диагностики

Ответьте на эти вопросы:

1. **Когда вы отключили Elementor?** (до или после начала атаки?)
2. **Удаляли ли вы файлы Elementor через SFTP?** (или только отключили через админку?)
3. **Что показывает Wordfence в логах?** (блокирует ли он эти запросы?)
4. **Есть ли Elementor на других ваших сайтах?** (mirecuerdo.es?)
5. **Когда последний раз обновляли Wordfence?**
6. **Что за процесс `python bot_personal.py`?** (это ваш скрипт?)

Эти ответы помогут понять, в чем именно проблема.




