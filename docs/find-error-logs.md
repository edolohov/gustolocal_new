# Где найти логи ошибок, если нет error_log в корне

## Проблема
Файл `error_log` отсутствует в корне сайта. Это нормально - он создается автоматически только при наличии ошибок или если настроено логирование.

---

## 🔍 Где искать логи

### 1. Через панель хостинга (самый простой способ)

**Обычные места в панели:**
- **cPanel:** `Metrics` → `Errors` или `Error Log`
- **Plesk:** `Logs` → `Error Log`
- **ISPmanager:** `Логи` → `Лог ошибок`
- **Другие панели:** Ищите раздел "Логи", "Error Logs", "Журналы"

**Что делать:**
1. Войдите в панель хостинга
2. Найдите раздел "Логи" или "Error Logs"
3. Выберите ваш домен `gustolocal.es`
4. Посмотрите последние записи

---

### 2. Альтернативные пути через SFTP/SSH

Файл `error_log` может быть в разных местах:

```bash
# В корне сайта (может не быть, если нет ошибок)
/home/s1149026/gustolocal.es/error_log

# В папке logs
/home/s1149026/gustolocal.es/logs/error_log
/home/s1149026/logs/error_log
/home/s1149026/logs/gustolocal.es/error_log

# В общей папке логов хостинга
/home/s1149026/logs/php_error.log
/home/s1149026/logs/error_log

# В папке public_html (если используется)
/home/s1149026/public_html/gustolocal.es/error_log
```

**Через SFTP:**
1. Подключитесь: `sftp://s1149026@141.8.192.20`
2. Проверьте папку `/home/s1149026/` - может быть папка `logs/`
3. Проверьте папку `/home/s1149026/gustolocal.es/` - может быть папка `logs/`

---

### 3. Логи веб-сервера (Apache/Nginx)

Эти логи находятся на уровне сервера и показывают все ошибки:

**Apache:**
```bash
# Через SSH (если есть доступ)
/var/log/apache2/error.log
/var/log/httpd/error_log
/home/s1149026/logs/apache_error.log
```

**Nginx:**
```bash
/var/log/nginx/error.log
/home/s1149026/logs/nginx_error.log
```

**Через панель хостинга:**
- Обычно в разделе "Логи" → "Apache Error Log" или "Nginx Error Log"

---

### 4. Логи PHP-FPM (если используется)

```bash
/var/log/php-fpm/error.log
/var/log/php8.1-fpm.log  # версия может отличаться
/home/s1149026/logs/php-fpm.log
```

---

## ✅ Как создать и настроить error_log

### Способ 1: Через wp-config.php (рекомендуется)

Добавьте в **самое начало** файла `wp-config.php` (сразу после `<?php`):

```php
<?php
// Принудительное создание и настройка error_log
$error_log_path = '/home/s1149026/gustolocal.es/error_log';
ini_set('log_errors', 1);
ini_set('error_log', $error_log_path);
ini_set('error_reporting', E_ALL);
ini_set('display_errors', 0);

// Создать файл, если его нет (с правами записи)
if (!file_exists($error_log_path)) {
    touch($error_log_path);
    chmod($error_log_path, 0644);
}

// Остальной код wp-config.php...
```

### Способ 2: Через .htaccess

Добавьте в `.htaccess` в корне WordPress:

```apache
# Настройка логирования PHP ошибок
php_value log_errors On
php_value error_log /home/s1149026/gustolocal.es/error_log
php_value error_reporting E_ALL
php_value display_errors Off
```

**Внимание:** Этот способ работает только если хостинг разрешает изменять PHP настройки через .htaccess.

### Способ 3: Через php.ini (если есть доступ)

Если у вас есть доступ к `php.ini` или можете создать `.user.ini`:

```ini
log_errors = On
error_log = /home/s1149026/gustolocal.es/error_log
error_reporting = E_ALL
display_errors = Off
```

---

## 🔧 Быстрая диагностика: создайте тестовый файл

Создайте файл `test-error-log.php` в корне WordPress:

```php
<?php
// test-error-log.php - тест логирования ошибок

// Настройка логирования
$error_log_path = __DIR__ . '/error_log';
ini_set('log_errors', 1);
ini_set('error_log', $error_log_path);
ini_set('error_reporting', E_ALL);
ini_set('display_errors', 1);

echo "<h1>Тест логирования ошибок</h1>";

// Проверка текущих настроек
echo "<h2>Текущие настройки PHP:</h2>";
echo "<pre>";
echo "log_errors: " . ini_get('log_errors') . "\n";
echo "error_log: " . ini_get('error_log') . "\n";
echo "error_reporting: " . ini_get('error_reporting') . "\n";
echo "display_errors: " . ini_get('display_errors') . "\n";
echo "</pre>";

// Попытка создать error_log
if (!file_exists($error_log_path)) {
    if (touch($error_log_path)) {
        chmod($error_log_path, 0644);
        echo "<p style='color: green;'>✅ Файл error_log создан: $error_log_path</p>";
    } else {
        echo "<p style='color: red;'>❌ Не удалось создать файл error_log</p>";
        echo "<p>Проверьте права доступа к папке</p>";
    }
} else {
    echo "<p style='color: green;'>✅ Файл error_log уже существует: $error_log_path</p>";
    echo "<p>Размер: " . filesize($error_log_path) . " байт</p>";
}

// Генерируем тестовую ошибку
echo "<h2>Генерация тестовой ошибки...</h2>";
trigger_error("Тестовая ошибка для проверки логирования - " . date('Y-m-d H:i:s'), E_USER_WARNING);

// Показываем содержимое error_log
if (file_exists($error_log_path)) {
    echo "<h2>Последние 20 строк error_log:</h2>";
    $lines = file($error_log_path);
    $last_lines = array_slice($lines, -20);
    echo "<pre style='background: #f5f5f5; padding: 10px;'>";
    echo htmlspecialchars(implode('', $last_lines));
    echo "</pre>";
} else {
    echo "<p style='color: red;'>❌ Файл error_log не найден после создания</p>";
}

// Показываем альтернативные пути
echo "<h2>Альтернативные пути для логов:</h2>";
$alternative_paths = [
    __DIR__ . '/error_log',
    __DIR__ . '/logs/error_log',
    dirname(__DIR__) . '/error_log',
    dirname(__DIR__) . '/logs/error_log',
    sys_get_temp_dir() . '/php_errors.log',
];

echo "<ul>";
foreach ($alternative_paths as $path) {
    $exists = file_exists($path);
    $status = $exists ? "✅ Существует" : "❌ Не найден";
    echo "<li>$path - $status</li>";
}
echo "</ul>";

echo "<hr>";
echo "<p><strong>⚠️ УДАЛИТЕ этот файл после тестирования!</strong></p>";
?>
```

**Использование:**
1. Загрузите файл в корень WordPress через SFTP
2. Откройте: `https://gustolocal.es/test-error-log.php`
3. Скрипт покажет, где создается/находится error_log
4. **Удалите файл после использования!**

---

## 📋 Пошаговая инструкция для вашего случая

### Шаг 1: Проверьте панель хостинга
1. Войдите в панель хостинга
2. Найдите раздел "Логи" или "Error Logs"
3. Выберите домен `gustolocal.es`
4. Посмотрите последние записи

### Шаг 2: Добавьте настройки в wp-config.php

Добавьте в **самое начало** `wp-config.php`:

```php
<?php
// Настройка логирования ошибок
ini_set('log_errors', 1);
ini_set('error_log', '/home/s1149026/gustolocal.es/error_log');
ini_set('error_reporting', E_ALL);
ini_set('display_errors', 0);
ini_set('max_execution_time', 120);

// Остальной код...
```

### Шаг 3: Создайте файл error_log вручную (через SFTP)

1. Подключитесь через SFTP
2. Перейдите в `/home/s1149026/gustolocal.es/`
3. Создайте пустой файл `error_log`
4. Установите права: `644` (rw-r--r--)

### Шаг 4: Используйте тестовый скрипт

Используйте `test-error-log.php` для проверки, где создается error_log.

---

## 🔍 Команды для поиска логов (если есть SSH доступ)

```bash
# Найти все error_log файлы
find /home/s1149026 -name "error_log" -type f 2>/dev/null

# Найти все .log файлы
find /home/s1149026 -name "*.log" -type f -mtime -1 2>/dev/null

# Проверить логи в папке logs
ls -la /home/s1149026/logs/

# Посмотреть последние ошибки Apache
tail -100 /var/log/apache2/error.log 2>/dev/null || tail -100 /var/log/httpd/error_log 2>/dev/null
```

---

## 💡 Важно

1. **Файл error_log создается автоматически** при первой ошибке, если настроено логирование
2. **Проверьте панель хостинга** - там обычно есть все логи в удобном виде
3. **Логи Apache/Nginx** показывают ошибки на уровне веб-сервера
4. **После настройки логирования** попробуйте открыть сайт - файл должен создаться автоматически

---

## 🆘 Если ничего не помогает

1. **Обратитесь в поддержку хостинга** - они точно знают, где находятся логи для вашего аккаунта
2. **Спросите у них:**
   - Где находятся логи PHP ошибок?
   - Где находятся логи Apache?
   - Как включить логирование ошибок?




