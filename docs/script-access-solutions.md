# 🔧 Решения проблемы доступа к скрипту (Forbidden)

## Проблема
Скрипт блокируется везде: в корне, в wp-content/uploads/, везде Forbidden.

**Причина:** Apache блокирует выполнение PHP файлов, которых нет в белом списке `.htaccess`.

---

## ✅ РЕШЕНИЕ 1: Переименовать скрипт в имя из белого списка (РЕКОМЕНДУЕТСЯ)

Самый простой способ - использовать имя файла, которое уже разрешено в `.htaccess`.

### Шаг 1: Проверьте, существует ли файл admin-ajax.php

Через SFTP проверьте:
```
/home/s1149026/gustolocal.es/admin-ajax.php
```

### Вариант A: Если файл НЕ существует

1. **Создайте упрощенный скрипт:**
   - Используйте `docs/find-backdoor-simple.php`
   - Переименуйте его в `admin-ajax.php`

2. **Загрузите в корень WordPress:**
   ```
   /home/s1149026/gustolocal.es/admin-ajax.php
   ```

3. **Откройте в браузере:**
   ```
   https://gustolocal.es/admin-ajax.php?key=CHANGE_THIS_PASSWORD
   ```

4. **ВАЖНО:** После использования удалите файл!

### Вариант B: Если файл существует

Используйте другое имя из белого списка, например:
- `wp-load.php` (если не используется)
- `wp-cron.php` (временно)
- `xmlrpc.php` (временно)

**ВНИМАНИЕ:** Если используете существующий файл - сделайте бэкап!

---

## ✅ РЕШЕНИЕ 2: Добавить правило в .htaccess (если есть доступ)

Если можете редактировать корневой `.htaccess`:

### Шаг 1: Найдите корневой .htaccess

Файл должен быть здесь:
```
/home/s1149026/gustolocal.es/.htaccess
```

**НЕ файл в `gustolocal-child/.htaccess`!**

### Шаг 2: Добавьте правило

Добавьте в **самое начало** файла (перед всеми остальными правилами):

```apache
# ВРЕМЕННОЕ РАЗРЕШЕНИЕ ДЛЯ СКРИПТА ПОИСКА БЭКДОРОВ
<FilesMatch '^(find-backdoor-script\.php|find-backdoor-simple\.php)$'>
Order allow,deny
Allow from all
</FilesMatch>
```

### Шаг 3: Сохраните и попробуйте снова

```
https://gustolocal.es/find-backdoor-script.php?key=CHANGE_THIS_PASSWORD
```

### Шаг 4: Удалите правило после использования

**ВАЖНО:** После использования скрипта удалите это правило из `.htaccess`!

---

## ✅ РЕШЕНИЕ 3: Использовать упрощенный скрипт через админку WordPress

Если у вас есть доступ к админке WordPress:

### Шаг 1: Создайте простой плагин

Создайте файл `wp-content/plugins/find-backdoor-temp.php`:

```php
<?php
/**
 * Plugin Name: Find Backdoor Temp
 * Description: Временный плагин для поиска бэкдоров
 * Version: 1.0
 */

// Защита
if (!defined('ABSPATH')) exit;

add_action('admin_menu', function() {
    add_menu_page(
        'Find Backdoor',
        'Find Backdoor',
        'manage_options',
        'find-backdoor',
        'find_backdoor_page'
    );
});

function find_backdoor_page() {
    if (!current_user_can('manage_options')) return;
    
    // Здесь код поиска из find-backdoor-script.php
    // (скопируйте логику поиска)
    
    echo '<h1>Поиск бэкдоров</h1>';
    echo '<p>Результаты поиска...</p>';
}
```

### Шаг 2: Активируйте плагин

WordPress → Плагины → Find Backdoor Temp → Активировать

### Шаг 3: Используйте

WordPress → Find Backdoor → Просмотрите результаты

### Шаг 4: Удалите плагин

После использования деактивируйте и удалите плагин!

---

## ✅ РЕШЕНИЕ 4: Использовать через SSH (если есть доступ)

Если у вас есть SSH доступ:

```bash
cd /home/s1149026/gustolocal.es

# Поиск известных вредоносных файлов
find . -name "postnews.php" -type f
find . -name "eDE9CW.php" -type f
find . -path "*/cgi-bin/*" -type f
find . -path "*/images/images/*" -type f

# Поиск файлов с подозрительным кодом
grep -r "eval(" --include="*.php" . | head -20
grep -r "base64_decode" --include="*.php" . | head -20
```

---

## ✅ РЕШЕНИЕ 5: Ручной поиск через SFTP (БЕЗ СКРИПТА)

Если ничего не работает, используйте ручной поиск:

### Шаг 1: Через SFTP найдите и удалите известные файлы

**Известные вредоносные файлы (безопасно удалить):**

1. **postnews.php** (4 файла):
   - `/postnews.php`
   - `/wp-admin/postnews.php`
   - `/wp-content/postnews.php`
   - `/staging/postnews.php`

2. **eDE9CW.php**:
   - `/wp-admin/eDE9CW.php`

3. **Папка cgi-bin/**:
   - `/cgi-bin/` (удалите всю папку)

4. **Папка images/images/**:
   - `/images/images/` (удалите всю папку)

5. **Папка wp-admin/css/css/**:
   - `/wp-admin/css/css/` (удалите всю папку)

### Шаг 2: Проверьте подозрительные вложенные папки

Ищите папки с двойными именами:
- `images/images/`
- `css/css/`
- `library/library/`
- `textcolor/textcolor/`
- `SecretStream/SecretStream/`

### Шаг 3: Проверьте файлы в wp-includes/

Ищите файлы в нестандартных местах:
- `wp-includes/Requests/library/library/` (должно быть только `library/`)
- `wp-includes/js/tinymce/plugins/textcolor/textcolor/` (должно быть только `textcolor/`)

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПЛАН

1. **Попробуйте Решение 1** (переименовать в `admin-ajax.php`) - самое простое
2. **Если не работает** - используйте Решение 5 (ручной поиск через SFTP)
3. **После очистки** - попробуйте сканеры снова

---

## 📋 ЧЕК-ЛИСТ

- [ ] Проверено, существует ли `admin-ajax.php`
- [ ] Создан упрощенный скрипт `find-backdoor-simple.php`
- [ ] Переименован в `admin-ajax.php` (или другое имя из белого списка)
- [ ] Загружен в корень WordPress
- [ ] Открыт в браузере с правильным паролем
- [ ] Получены результаты
- [ ] Скрипт удален после использования
- [ ] Удалены найденные вредоносные файлы

---

## ⚠️ ВАЖНО

1. **Удалите скрипт после использования** - это критично для безопасности!
2. **Если используете существующий файл** - сделайте бэкап!
3. **Если добавляете правило в .htaccess** - удалите его после использования!
4. **Пароль по умолчанию:** `CHANGE_THIS_PASSWORD`

---

## 🔗 Полезные файлы

- **Упрощенный скрипт**: `docs/find-backdoor-simple.php`
- **Полный скрипт**: `docs/find-backdoor-script.php`
- **Список файлов для удаления**: `docs/cleanup-gustolocal-analysis.md`




