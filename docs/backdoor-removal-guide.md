# 🚨 Руководство по удалению активного бэкдора

## Проблема
Файлы удаляются, но сразу появляются снова. Это означает, что на сервере работает **активный бэкдор**, который автоматически воссоздает зараженные файлы.

---

## 🔍 Шаг 1: Найти источник проблемы

### Вариант A: Использовать диагностический скрипт (рекомендуется)

1. Загрузите файл `find-backdoor-script.php` в корень сайта:
   ```
   /home/s1149026/gustolocal.es/find-backdoor-script.php
   ```

2. Откройте в браузере:
   ```
   https://gustolocal.es/find-backdoor-script.php?key=YOUR_PASSWORD
   ```
   (Замените `YOUR_PASSWORD` на свой пароль - он указан в начале файла)

3. Скрипт найдет:
   - Файлы, которые создают папки `cgi-bin`
   - Подозрительные файлы с вредоносным кодом
   - Cron задачи, которые могут воссоздавать файлы
   - Подозрительные .htaccess файлы
   - Недавно измененные PHP файлы

### Вариант B: Ручной поиск через SFTP

Ищите файлы, которые содержат:
- `mkdir.*cgi-bin`
- `file_put_contents.*cgi-bin`
- `fwrite.*cgi-bin`
- Любые упоминания `cgi-bin` в PHP коде

**Типичные места, где может быть бэкдор:**
- `/wp-content/themes/*/functions.php` (в темах)
- `/wp-content/plugins/*/` (в плагинах)
- `/wp-config.php` (в конце файла)
- `/wp-load.php` (модифицированный)
- `/wp-includes/*.php` (модифицированные файлы ядра)
- Корневые PHP файлы с подозрительными именами

---

## 🎯 Шаг 2: Проверить cron задачи

Бэкдор может быть установлен как cron задача, которая запускается каждые несколько минут.

### Через панель хостинга:
1. Войдите в панель хостинга
2. Найдите раздел "Cron задачи" или "Планировщик задач"
3. Проверьте все задачи на наличие подозрительных команд
4. Удалите все подозрительные задачи

### Через SSH (если доступен):
```bash
crontab -l
```

Ищите строки, содержащие:
- `cgi-bin`
- `postnews.php`
- `cache.php`
- Подозрительные PHP файлы
- Команды `wget`, `curl` с подозрительными URL

---

## 🔧 Шаг 3: Проверить права доступа

Убедитесь, что права доступа правильные:

```bash
# Файлы должны быть 644
find /home/s1149026/gustolocal.es -type f -name "*.php" -exec chmod 644 {} \;

# Папки должны быть 755
find /home/s1149026/gustolocal.es -type d -exec chmod 755 {} \;

# wp-config.php должен быть 600 (только владелец может читать)
chmod 600 /home/s1149026/gustolocal.es/wp-config.php
```

---

## 🗑️ Шаг 4: Удалить бэкдор и зараженные файлы

### 4.1. Удалить все зараженные файлы одновременно

**Через SFTP:**
1. Удалите все файлы из списка `list_07_11_hackscan_resul.txt`
2. Удалите всю папку `cgi-bin/` полностью
3. Удалите папки `images/images/` если они есть

**Через SSH (если доступен):**
```bash
cd /home/s1149026/gustolocal.es

# Удалить все postnews.php
find . -name "postnews.php" -type f -delete

# Удалить eDE9CW.php
find . -name "eDE9CW.php" -type f -delete

# Удалить всю папку cgi-bin
rm -rf cgi-bin/

# Удалить подозрительные папки images/images
rm -rf images/images/

# Удалить подозрительные папки в wp-admin
rm -rf wp-admin/css/css/

# Удалить подозрительные файлы в wp-includes
find wp-includes/ -path "*/library/library/*" -delete
find wp-includes/ -path "*/textcolor/textcolor/*" -delete
find wp-includes/ -path "*/SecretStream/SecretStream/*" -delete
```

### 4.2. Найти и удалить бэкдор

После того, как скрипт найдет подозрительные файлы:

1. **Сделайте бэкап** подозрительных файлов (на всякий случай)
2. **Проверьте содержимое** файлов через SFTP
3. **Удалите** файлы, которые содержат код создания `cgi-bin`
4. **Проверьте** файлы, которые были изменены недавно (последние 7 дней)

---

## 🔒 Шаг 5: Заблокировать воссоздание файлов

### 5.1. Создать .htaccess для блокировки

Создайте файл `/home/s1149026/gustolocal.es/.htaccess` (или добавьте в существующий):

```apache
# БЛОКИРОВКА СОЗДАНИЯ ЗАРАЖЕННЫХ ФАЙЛОВ

# Блокировать доступ к cgi-bin
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteRule ^cgi-bin/ - [F,L]
RewriteRule ^.*/cgi-bin/ - [F,L]
</IfModule>

# Блокировать выполнение postnews.php
<Files "postnews.php">
Order allow,deny
Deny from all
</Files>

# Блокировать выполнение eDE9CW.php
<Files "eDE9CW.php">
Order allow,deny
Deny from all
</Files>

# Блокировать выполнение cache.php в нестандартных местах
<FilesMatch "cache\.php$">
<If "%{REQUEST_URI} !~ m#^(/wp-content/plugins/[^/]+/cache\.php|/wp-content/themes/[^/]+/cache\.php)$#">
Order allow,deny
Deny from all
</IfModule>
</FilesMatch>
```

### 5.2. Создать защиту на уровне файловой системы

Если есть доступ к SSH, создайте защищенные папки:

```bash
# Создать папку cgi-bin и сделать её недоступной
mkdir -p /home/s1149026/gustolocal.es/cgi-bin
chmod 000 /home/s1149026/gustolocal.es/cgi-bin
chattr +i /home/s1149026/gustolocal.es/cgi-bin  # Сделать неизменяемой (если поддерживается)
```

---

## 🔍 Шаг 6: Проверить wp-config.php

Бэкдор часто добавляется в конец `wp-config.php`.

1. Откройте `/home/s1149026/gustolocal.es/wp-config.php` через SFTP
2. Проверьте **конец файла** (после строки `/* That's all, stop editing! */`)
3. Если там есть подозрительный код - удалите его
4. Файл должен заканчиваться на:
   ```php
   /* That's all, stop editing! Happy publishing. */
   ```

---

## 🛡️ Шаг 7: Усилить безопасность

### 7.1. Установить/обновить Wordfence

1. Войдите в админку WordPress
2. Установите плагин **Wordfence Security**
3. Запустите полное сканирование
4. Включите Firewall

### 7.2. Сменить все пароли

- Пароль администратора WordPress
- Пароль FTP/SFTP
- Пароль базы данных
- Пароль панели хостинга

### 7.3. Проверить пользователей WordPress

1. В админке: **Пользователи** → **Все пользователи**
2. Удалите всех подозрительных пользователей
3. Проверьте роли администраторов

---

## 📋 Шаг 8: Мониторинг

После удаления бэкдора:

1. **Проверяйте** папку `cgi-bin/` каждые несколько часов
2. Если файлы появляются снова - бэкдор не удален полностью
3. Используйте скрипт `find-backdoor-script.php` снова для поиска

---

## ⚠️ Если файлы продолжают появляться

Если после всех действий файлы продолжают появляться:

1. **Обратитесь в поддержку хостинга** - возможно, бэкдор на уровне сервера
2. **Проверьте другие сайты** на том же хостинге
3. **Рассмотрите возможность** временного отключения сайта для полной очистки
4. **Проверьте логи доступа** - возможно, атака идет извне постоянно

---

## 🆘 Экстренные меры

Если сайт критически важен и нужно быстро заблокировать:

1. **Временно отключите PHP** в папке `cgi-bin/`:
   ```apache
   # В .htaccess
   <Directory "/home/s1149026/gustolocal.es/cgi-bin">
       php_flag engine off
   </Directory>
   ```

2. **Заблокируйте IP адреса** атакующих (из логов хостинга)

3. **Ограничьте доступ** к админке WordPress по IP

---

## ✅ Чек-лист после очистки

- [ ] Найден и удален бэкдор
- [ ] Удалены все зараженные файлы
- [ ] Проверены и очищены cron задачи
- [ ] Проверен wp-config.php
- [ ] Установлены правила в .htaccess
- [ ] Сменены все пароли
- [ ] Установлен/обновлен Wordfence
- [ ] Проверены пользователи WordPress
- [ ] Сайт работает нормально
- [ ] Файлы не появляются снова (проверка через 24 часа)




