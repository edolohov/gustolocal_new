# ✅ Быстрый чек-лист исправления Wordfence

## 🚨 КРИТИЧНО: Исправьте wp-config.php СЕЙЧАС!

### Проблема
Вы определили константы памяти **ДВАЖДЫ** в wp-config.php. Первое определение имеет приоритет, поэтому WordPress использует только **256M/512M** вместо **512M/1024M**.

### Решение (2 минуты)

1. **Откройте wp-config.php через SFTP**

2. **Найдите эти строки (обычно строка ~73):**
   ```php
   define( 'WP_MEMORY_LIMIT', '256M' );
   define( 'WP_MAX_MEMORY_LIMIT', '512M' );
   ```

3. **Замените на:**
   ```php
   define( 'WP_MEMORY_LIMIT', '512M' );
   define( 'WP_MAX_MEMORY_LIMIT', '1024M' );
   ```

4. **Найдите секцию Wordfence и удалите дублирующие строки:**
   ```php
   // УДАЛИТЕ ЭТИ СТРОКИ (они не работают):
   define('WP_MEMORY_LIMIT', '512M');      // ← УДАЛИТЬ
   define('WP_MAX_MEMORY_LIMIT', '1024M'); // ← УДАЛИТЬ
   ```

5. **Оставьте только:**
   ```php
   @ini_set('max_execution_time', 600);
   @set_time_limit(600);
   @ini_set('memory_limit', '1024M');
   ```

6. **Сохраните файл**

---

## ⚙️ Настройки Wordfence (1 минута)

1. **Wordfence → Scan → Manage Scan**

2. **Измените:**
   - "Максимальное время для каждого этапа сканирования" = **60** (было 15) ✅
   - "Какой объем памяти должен запрашивать Wordfence" = **512** (было 256) ✅
   - "Использовать сканирование с низким уровнем ресурсов" = **ВКЛЮЧЕНО** ✅

3. **Сохраните изменения**

---

## 🧹 Очистка от вредоносного ПО (10-15 минут)

**Вредоносный код может блокировать сканирование!**

### Быстрое удаление известных файлов:

Удалите через SFTP:
- `/postnews.php` (корневой)
- `/wp-admin/postnews.php`
- `/wp-content/postnews.php`
- `/wp-admin/eDE9CW.php`
- `/cgi-bin/` (всю папку)
- `/images/images/` (всю папку)
- `/wp-admin/css/css/` (всю папку)

**Всего: ~29 файлов можно удалить безопасно**

Подробный список: см. `docs/cleanup-gustolocal-analysis.md`

---

## 🔄 После исправлений

1. **Подождите 5 минут** (чтобы изменения применились)
2. **Запустите сканирование Wordfence**
3. **Если все еще зависает** - используйте альтернативный сканер:
   - Sucuri Security (плагин)
   - MalCare Security (плагин)
   - Онлайн-сканер: https://sitecheck.sucuri.net/

---

## 📊 Ожидаемый результат

После исправлений:
- ✅ WordPress будет использовать 512M/1024M памяти
- ✅ Каждый этап сканирования будет работать до 60 секунд
- ✅ Wordfence запросит 512MB памяти для сканирования
- ✅ Вредоносные файлы не будут блокировать сканирование

---

## ⚠️ Если проблема сохраняется

1. **Проверьте cron задачи** - вредоносный код может воссоздавать файлы
2. **Используйте скрипт поиска бэкдоров**: `docs/find-backdoor-script.php`
3. **Обратитесь в поддержку хостинга** - возможно, ограничения на уровне сервера
4. **Используйте альтернативный сканер** (Sucuri/MalCare)

---

## 📝 Полная инструкция

См. подробную инструкцию: `docs/wordfence-scan-fix-infected-site.md`




