# 🔍 Ручной поиск бэкдора (без загрузки скрипта)

Если скрипт блокируется сервером, используйте эти методы для поиска бэкдора.

---

## Метод 1: Поиск через SFTP/File Manager

### Шаг 1: Проверьте wp-config.php

**Это самое важное!** Бэкдор часто добавляется в конец `wp-config.php`.

1. Откройте через SFTP: `/home/s1149026/gustolocal.es/wp-config.php`
2. Прокрутите до **самого конца файла**
3. Файл должен заканчиваться на:
   ```php
   /* That's all, stop editing! Happy publishing. */
   ```
4. Если после этой строки есть **любой другой код** - это бэкдор!
5. Удалите весь код после `/* That's all, stop editing! */`

**Что искать:**
- `eval(`
- `base64_decode(`
- `exec(`
- `system(`
- `shell_exec(`
- `file_put_contents(`
- `mkdir(`
- Любые упоминания `cgi-bin`
- Длинные строки base64

---

### Шаг 2: Проверьте functions.php в темах

1. Откройте через SFTP папку: `/home/s1149026/gustolocal.es/wp-content/themes/`
2. Для **каждой темы** проверьте файл `functions.php`
3. Ищите в файле:
   - `cgi-bin`
   - `postnews.php`
   - `eval(`
   - `base64_decode(`
   - `file_put_contents(`
   - `mkdir(`
   - Подозрительные функции

**Особое внимание:**
- Проверьте **конец файла** `functions.php` - бэкдор часто добавляется в конец
- Проверьте файлы в подпапках тем (например, `inc/functions.php`)

---

### Шаг 3: Проверьте недавно измененные файлы

1. В SFTP клиенте отсортируйте файлы по **дате изменения**
2. Проверьте все PHP файлы, измененные **после 03/11/2025** (дата заражения)
3. Особое внимание к файлам в:
   - `/wp-content/themes/`
   - `/wp-content/plugins/`
   - `/wp-includes/` (если есть измененные)
   - Корне сайта

---

### Шаг 4: Поиск по содержимому файлов

Используйте поиск в SFTP клиенте (если поддерживается) или проверьте вручную:

**Ищите файлы, содержащие:**
- `mkdir.*cgi-bin`
- `file_put_contents.*cgi-bin`
- `fwrite.*cgi-bin`
- `copy.*cgi-bin`
- `exec.*cgi-bin`
- `system.*cgi-bin`
- `shell_exec.*cgi-bin`

**Где искать:**
1. `/wp-content/themes/*/functions.php`
2. `/wp-content/plugins/*/` (проверьте все плагины)
3. `/wp-config.php`
4. Корневые PHP файлы

---

## Метод 2: Проверка через панель хостинга

### Проверка cron задач

1. Войдите в панель хостинга
2. Найдите раздел **"Cron задачи"** или **"Планировщик задач"**
3. Проверьте **все** задачи на наличие:
   - `cgi-bin`
   - `postnews.php`
   - `cache.php`
   - Подозрительные PHP файлы
   - Команды `wget` или `curl` с подозрительными URL
4. **Удалите все подозрительные задачи**

---

### Проверка логов доступа

1. В панели хостинга найдите раздел **"Логи"** или **"Access Logs"**
2. Ищите запросы к:
   - `/wp-admin/eDE9CW.php`
   - `/postnews.php`
   - `/cgi-bin/`
3. Найдите IP адреса, которые обращаются к этим файлам
4. Проверьте, не ваш ли это IP

---

## Метод 3: Альтернативное размещение скрипта

Если скрипт блокируется в корне, попробуйте разместить его в другом месте:

### Вариант A: В папке wp-content

1. Загрузите скрипт в: `/home/s1149026/gustolocal.es/wp-content/find-backdoor.php`
2. Откройте: `https://gustolocal.es/wp-content/find-backdoor.php?key=YOUR_PASSWORD`

### Вариант B: В папке uploads

1. Загрузите скрипт в: `/home/s1149026/gustolocal.es/wp-content/uploads/find-backdoor.php`
2. Откройте: `https://gustolocal.es/wp-content/uploads/find-backdoor.php?key=YOUR_PASSWORD`

### Вариант C: Изменить имя файла

1. Переименуйте файл в: `check.php` или `test.php`
2. Загрузите в корень
3. Откройте: `https://gustolocal.es/check.php?key=YOUR_PASSWORD`

---

## Метод 4: Использование SSH (если доступен)

Если у вас есть доступ к SSH, используйте команды:

```bash
# Перейти в корень сайта
cd /home/s1149026/gustolocal.es

# Найти все файлы, содержащие "cgi-bin"
grep -r "cgi-bin" --include="*.php" .

# Найти все файлы, содержащие "mkdir.*cgi-bin"
grep -r "mkdir.*cgi-bin" --include="*.php" .

# Найти все файлы, содержащие "file_put_contents.*cgi-bin"
grep -r "file_put_contents.*cgi-bin" --include="*.php" .

# Найти все файлы с eval
grep -r "eval(" --include="*.php" . | grep -v "//"

# Найти все файлы с base64_decode
grep -r "base64_decode" --include="*.php" . | grep -v "//"

# Найти недавно измененные PHP файлы (последние 7 дней)
find . -name "*.php" -type f -mtime -7 -ls

# Найти файлы postnews.php
find . -name "postnews.php" -type f

# Найти файлы eDE9CW.php
find . -name "eDE9CW.php" -type f
```

---

## Метод 5: Проверка через базу данных

Бэкдор может быть в базе данных (в опциях WordPress).

1. Войдите в **phpMyAdmin**
2. Выберите базу данных вашего сайта
3. Выполните SQL запрос:

```sql
-- Поиск подозрительных опций
SELECT * FROM wp_options 
WHERE option_value LIKE '%cgi-bin%' 
   OR option_value LIKE '%postnews%'
   OR option_value LIKE '%eval%'
   OR option_value LIKE '%base64_decode%';
```

4. Если найдены подозрительные записи - удалите их или замените на пустые значения

---

## 🎯 Приоритетный план действий

### 1. СРОЧНО: Проверьте wp-config.php
   - Откройте файл через SFTP
   - Проверьте конец файла
   - Удалите весь код после `/* That's all, stop editing! */`

### 2. Проверьте functions.php во всех темах
   - Откройте каждую тему
   - Проверьте файл `functions.php`
   - Ищите упоминания `cgi-bin`, `postnews`, `eval`, `base64_decode`

### 3. Проверьте cron задачи
   - Войдите в панель хостинга
   - Проверьте все cron задачи
   - Удалите подозрительные

### 4. Проверьте недавно измененные файлы
   - Отсортируйте по дате изменения
   - Проверьте файлы, измененные после 03/11/2025

### 5. Попробуйте альтернативное размещение скрипта
   - Загрузите в `wp-content/` или `wp-content/uploads/`
   - Или переименуйте файл

---

## ⚠️ Что делать, если нашли бэкдор

1. **Сделайте бэкап** файла (на всякий случай)
2. **Удалите** вредоносный код из файла
3. Если файл полностью заражен - **замените** его на чистую версию
4. **Удалите** все зараженные файлы (`postnews.php`, `cgi-bin/`, и т.д.)
5. **Проверьте**, что файлы не появляются снова
6. **Смените все пароли**

---

## 🔒 После удаления бэкдора

1. Установите/обновите **Wordfence Security**
2. Запустите полное сканирование
3. Добавьте правила блокировки в `.htaccess`
4. Смените все пароли
5. Проверьте пользователей WordPress




