# Решение проблемы: Wordfence сканирование не работает

## Проблема
Ошибка: "The current scan looks like it has failed. Its last status update was 7 mins ago."

Это происходит из-за того, что хостинг останавливает процессы, которые работают дольше установленного лимита времени, или из-за ограничений памяти.

---

## ✅ РЕШЕНИЕ 1: Настройка через wp-config.php (РЕКОМЕНДУЕТСЯ)

Это самый надежный способ, который работает на большинстве хостингов.

### Шаг 1: Найдите файл wp-config.php
Файл находится в корневой директории WordPress (там же, где `wp-content`, `wp-admin`).

### Шаг 2: Добавьте настройки

Добавьте следующие строки в файл `wp-config.php` **ПЕРЕД** строкой `/* That's all, stop editing! Happy publishing. */`:

```php
// ============================================
// НАСТРОЙКИ ДЛЯ WORDFENCE СКАНИРОВАНИЯ
// ============================================
// Увеличиваем лимит времени выполнения до 10 минут (600 секунд)
@ini_set('max_execution_time', 600);
@set_time_limit(600);

// Увеличиваем лимит памяти (если еще не установлено)
define('WP_MEMORY_LIMIT', '512M');
define('WP_MAX_MEMORY_LIMIT', '1024M');
@ini_set('memory_limit', '1024M');

// Отключаем ограничение времени для cron задач (важно для Wordfence)
define('DISABLE_WP_CRON', false);
```

### Пример полного блока в wp-config.php:

```php
define('WP_HOME', 'https://gustolocal.es');
define('WP_SITEURL', 'https://gustolocal.es');

// ============================================
// НАСТРОЙКИ ПАМЯТИ
// ============================================
define('WP_MEMORY_LIMIT', '512M');
define('WP_MAX_MEMORY_LIMIT', '1024M');
@ini_set('memory_limit', '1024M');

// ============================================
// НАСТРОЙКИ ДЛЯ WORDFENCE СКАНИРОВАНИЯ
// ============================================
@ini_set('max_execution_time', 600);
@set_time_limit(600);

/* That's all, stop editing! Happy publishing. */
```

---

## ✅ РЕШЕНИЕ 2: Настройка через .htaccess

Если ваш хостинг поддерживает настройки PHP через `.htaccess`, добавьте в корневой `.htaccess` файл (в самом начале, перед другими директивами):

```apache
# Настройки для Wordfence сканирования
<IfModule mod_php7.c>
    php_value max_execution_time 600
    php_value memory_limit 1024M
</IfModule>

<IfModule mod_php8.c>
    php_value max_execution_time 600
    php_value memory_limit 1024M
</IfModule>
```

**Важно:** Если у вас уже есть `.htaccess` в корне WordPress, добавьте эти строки в самое начало файла.

---

## ✅ РЕШЕНИЕ 3: Настройка через php.ini (если есть доступ)

Если у вас есть доступ к файлу `php.ini` (через панель хостинга или через FTP):

```ini
max_execution_time = 600
memory_limit = 1024M
```

**Где найти php.ini:**
- В панели хостинга обычно есть раздел "PHP Settings" или "PHP Configuration"
- Или создайте файл `php.ini` в корневой директории WordPress

---

## ✅ РЕШЕНИЕ 4: Настройки в Wordfence

Также проверьте настройки самого плагина Wordfence:

1. Перейдите в **Wordfence → Scan → Manage Scan**
2. Найдите опцию **"Maximum execution time for each scan stage"**
3. Установите значение **300** (5 минут) или **600** (10 минут)
4. Сохраните изменения

---

## ✅ РЕШЕНИЕ 5: Использование низкоресурсного сканирования

Если проблема сохраняется, включите режим низкого потребления ресурсов:

1. Перейдите в **Wordfence → Scan → Manage Scan**
2. Включите опцию **"Use low resource scanning (reduces server load by lengthening the scan duration)"**
3. Сохраните изменения

---

## 🔍 Проверка текущих значений

Чтобы проверить текущие значения PHP, создайте временный файл `phpinfo.php` в корне WordPress:

```php
<?php
phpinfo();
?>
```

Откройте его в браузере: `https://gustolocal.es/phpinfo.php`

Найдите:
- `max_execution_time` - должно быть 600 или больше
- `memory_limit` - должно быть 1024M или больше

**ВАЖНО:** После проверки удалите файл `phpinfo.php` из соображений безопасности!

---

## 📋 Чек-лист решения проблемы

- [ ] Добавлены настройки в `wp-config.php`
- [ ] Добавлены настройки в `.htaccess` (если поддерживается)
- [ ] Проверены настройки в Wordfence плагине
- [ ] Включен режим низкого потребления ресурсов (если нужно)
- [ ] Проверены текущие значения через `phpinfo.php`
- [ ] Удален файл `phpinfo.php` после проверки
- [ ] Перезапущено сканирование Wordfence

---

## ⚠️ Если проблема сохраняется

Если после всех настроек проблема все еще возникает:

1. **Обратитесь в поддержку хостинга** - возможно, у них есть жесткие ограничения на уровне сервера, которые нельзя изменить через PHP настройки.

2. **Проверьте логи ошибок:**
   - `wp-content/debug.log` (если включен WP_DEBUG)
   - Логи хостинга в панели управления
   - Ищите сообщения о превышении времени выполнения или памяти

3. **Попробуйте сканировать в нерабочее время** - когда нагрузка на сервер меньше

4. **Обратитесь в поддержку Wordfence** - они могут предложить дополнительные решения для вашего конкретного случая

---

## 💡 Рекомендуемые значения

| Параметр | Минимальное значение | Рекомендуемое значение |
|----------|----------------------|------------------------|
| `max_execution_time` | 300 секунд (5 мин) | 600 секунд (10 мин) |
| `memory_limit` | 512M | 1024M |
| `WP_MEMORY_LIMIT` | 256M | 512M |
| `WP_MAX_MEMORY_LIMIT` | 512M | 1024M |

---

## 🔒 Безопасность

После добавления настроек убедитесь, что:
- Файл `wp-config.php` имеет права доступа `644` или `600`
- Файл `.htaccess` защищен от перезаписи (если возможно)
- Удален временный файл `phpinfo.php`




