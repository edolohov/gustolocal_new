# 🚨 СРОЧНЫЙ ПЛАН ДЕЙСТВИЙ: Защита от атаки на Elementor

## Ситуация
Ваш сайт подвергается **массовой атаке** через уязвимость в плагине Elementor. Все обработчики веб-сервера заняты, сайт недоступен.

## ⚡ ДЕЙСТВИЯ (выполните СРОЧНО по порядку)

### Шаг 1: Добавьте защиту в .htaccess (5 минут)

1. **Подключитесь через SFTP:** `sftp://s1149026@141.8.192.20`
2. **Перейдите в корень WordPress:** `/home/s1149026/gustolocal.es/`
3. **Откройте файл `.htaccess`** (или создайте, если его нет)
4. **Добавьте в САМОЕ НАЧАЛО файла** (перед всеми остальными правилами):

```apache
# БЛОКИРОВКА АТАКИ НА ELEMENTOR
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /

# Блокировка запросов с параметром ARRAY к animations/index.php
RewriteCond %{QUERY_STRING} ARRAY= [NC]
RewriteCond %{REQUEST_URI} animations/index\.php [NC]
RewriteRule .* - [F,L]

# Блокировка всех запросов к animations/index.php
RewriteCond %{REQUEST_URI} /wp-content/plugins/elementor/assets/lib/animations/animations/index\.php [NC]
RewriteRule .* - [F,L]

# Блокировка подозрительных запросов к Elementor
RewriteCond %{QUERY_STRING} (ARRAY|eval|base64|shell_exec|system|exec) [NC]
RewriteCond %{REQUEST_URI} elementor [NC]
RewriteRule .* - [F,L]
</IfModule>
```

5. **Сохраните файл**

**Готовый файл:** Используйте `docs/.htaccess-protection-elementor.txt` как шаблон

---

### Шаг 2: Временно отключите Elementor (если сайт все еще не работает)

**Через SFTP:**
1. Перейдите в `/home/s1149026/gustolocal.es/wp-content/plugins/`
2. **Переименуйте папку:** `elementor` → `elementor-disabled`
3. Сайт должен заработать (но без Elementor)

**Или через базу данных (если есть доступ к phpMyAdmin):**
```sql
UPDATE `dp___bk_250951_wp_options` 
SET `option_value` = REPLACE(`option_value`, 'elementor/elementor.php', '') 
WHERE `option_name` = 'active_plugins';
```

---

### Шаг 3: Обновите Elementor (когда сайт заработает)

1. Войдите в админку WordPress
2. Перейдите в **Плагины** → **Установленные плагины**
3. Найдите **Elementor**
4. Нажмите **Обновить** (если доступно)

**Или удалите и установите заново:**
- Удалите старую версию через SFTP
- Установите последнюю версию через админку

---

### Шаг 4: Установите Wordfence Security (ОБЯЗАТЕЛЬНО!)

1. В админке WordPress: **Плагины** → **Добавить новый**
2. Найдите **Wordfence Security**
3. Установите и активируйте
4. Запустите **сканирование**
5. Включите **Firewall**
6. Настройте **Rate Limiting** (ограничение запросов)

---

### Шаг 5: Проверьте файл animations/index.php

**Через SFTP:**
1. Перейдите в `/home/s1149026/gustolocal.es/wp-content/plugins/elementor/assets/lib/animations/animations/`
2. Откройте файл `index.php`
3. **Он должен содержать только:**
   ```php
   <?php
   // Silence is golden.
   ```
4. Если там другой код - файл взломан, удалите плагин и установите заново

---

## 📊 Ответы на ваши вопросы

### Нужно ли менять хостинг?

**НЕТ!** Проблема не в хостинге. Хостинг просто показывает, что сервер перегружен атакой. После добавления защиты в `.htaccess` атака будет заблокирована.

### На чьей стороне проблема?

**На стороне сайта** - уязвимость в плагине Elementor или взломанный файл. Хостинг работает нормально, просто перегружен атакующими запросами.

### Что означает статус [B] (BUSY)?

Это означает, что обработчики веб-сервера заняты обработкой запросов. В вашем случае - атакующих запросов к Elementor.

---

## ✅ После выполнения шагов

1. **Сайт должен заработать** после добавления правил в `.htaccess`
2. **Атака будет заблокирована** на уровне веб-сервера
3. **Обновите Elementor** до последней версии
4. **Установите Wordfence** для долгосрочной защиты

---

## 🔍 Дополнительная диагностика

### Проверьте процесс python bot_personal.py

В логах виден процесс:
```
python bot_personal.py - работает 44 минуты
```

**Проверьте:**
1. Это ваш легитимный скрипт?
2. Если нет - найдите файл и проверьте его содержимое
3. Если это бэкдор - удалите его

**Где искать:**
- `/home/s1149026/gustolocal.es/`
- `/home/s1149026/`
- Любые `.py` файлы

---

## 📝 Чек-лист

- [ ] Добавлены правила блокировки в `.htaccess`
- [ ] Временно отключен Elementor (если нужно)
- [ ] Обновлен Elementor до последней версии
- [ ] Установлен Wordfence Security
- [ ] Проверен файл `animations/index.php`
- [ ] Проверен процесс `python bot_personal.py`

---

## 🆘 Если ничего не помогает

1. **Обратитесь в поддержку хостинга** с просьбой:
   - Временно заблокировать IP адреса атакующих
   - Помочь настроить mod_security правила
   - Проверить процесс `python bot_personal.py`

2. **Рассмотрите Cloudflare:**
   - Бесплатный план защищает от DDoS
   - Автоматически блокирует ботов
   - Кеширует контент

---

## ⚠️ ВАЖНО

1. **Действуйте быстро** - добавьте правила в `.htaccess` СЕЙЧАС
2. **Не паникуйте** - это решаемо
3. **Обновите Elementor** - это критично
4. **Установите защиту** - Wordfence обязателен

---

## 📞 Контакты

Если нужна помощь:
- Подробная инструкция: `docs/security-attack-elementor.md`
- Готовый .htaccess: `docs/.htaccess-protection-elementor.txt`




