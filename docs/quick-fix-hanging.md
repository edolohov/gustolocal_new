# Быстрое решение: Сайт зависает, в debug.log ничего нет

## Проблема
Сайт зависает, но в `debug.log` нет новых записей (последняя дата 7/15/25). Это означает, что ошибка происходит **до** того, как WordPress успевает залогировать.

---

## ✅ БЫСТРОЕ РЕШЕНИЕ (выполните по порядку)

### Шаг 1: Проверьте error_log PHP (НЕ debug.log!)

**Через SFTP:**
1. Подключитесь к серверу: `sftp://s1149026@141.8.192.20`
2. Перейдите в `/home/s1149026/gustolocal.es/`
3. Найдите файл `error_log` (в корне сайта)
4. Откройте и посмотрите **последние строки** - там должны быть свежие ошибки

**Или через панель хостинга:**
- Раздел "Логи" → "Error Log" или "PHP Error Log"

**Что искать:**
- `Fatal error`
- `Maximum execution time exceeded`
- `Allowed memory size exhausted`
- Имя плагина в стек-трейсе

---

### Шаг 2: Включите принудительное логирование PHP

Добавьте в **самое начало** файла `wp-config.php` (после `<?php`, перед всем остальным):

```php
<?php
// ВРЕМЕННО: Принудительное логирование PHP ошибок
ini_set('log_errors', 1);
ini_set('error_log', '/home/s1149026/gustolocal.es/error_log');
ini_set('error_reporting', E_ALL);
ini_set('display_errors', 0);
ini_set('max_execution_time', 120); // Увеличить время для диагностики

// Остальной код wp-config.php...
```

Это заставит PHP логировать ошибки **до** загрузки WordPress.

---

### Шаг 3: Отключите плагины через базу данных

Если сайт зависает при загрузке, WordPress не успевает выполнить код. Отключите плагины через БД:

**Через phpMyAdmin:**

1. Откройте phpMyAdmin
2. Выберите базу данных: `s1149026_gusto`
3. Найдите таблицу: `dp___bk_250951_wp_options`
4. Найдите строку с `option_name = 'active_plugins'`
5. Измените `option_value` на: `a:0:{}`
6. Сохраните

**Или выполните SQL:**
```sql
UPDATE `dp___bk_250951_wp_options` 
SET `option_value` = 'a:0:{}' 
WHERE `option_name` = 'active_plugins';
```

После этого сайт должен загрузиться без плагинов.

---

### Шаг 4: Используйте тестовый скрипт

1. Загрузите файл `test-load.php` в корень WordPress через SFTP
2. Откройте в браузере: `https://gustolocal.es/test-load.php`
3. Скрипт покажет, на каком этапе происходит зависание
4. **Удалите файл после использования!**

---

### Шаг 5: Проверьте must-use плагины

Плагины в папке `mu-plugins` загружаются автоматически и не отключаются через админку.

**Через SFTP:**
1. Перейдите в `/home/s1149026/gustolocal.es/wp-content/`
2. Если есть папка `mu-plugins` - переименуйте её в `mu-plugins-disabled`
3. Проверьте, загружается ли сайт

---

### Шаг 6: Проверьте логи Apache

**Через панель хостинга:**
- Раздел "Логи" → "Apache Error Log"

**Или через SSH:**
```bash
tail -100 /var/log/apache2/error.log
```

**Что искать:**
- `timeout`
- `Premature end of script headers`
- `mod_php`

---

## 🔍 Диагностика по результатам

### Если сайт загрузился после отключения плагинов:
✅ Проблема в одном из плагинов
- Включайте плагины по одному через админку
- Или переименовывайте папки плагинов по одной через SFTP

### Если сайт все еще зависает:
- Проверьте `error_log` (не `debug.log`)
- Проверьте логи Apache
- Переименуйте активную тему
- Переименуйте `functions.php` темы

### Если в error_log есть ошибки:
- Найдите имя плагина/файла в стек-трейсе
- Отключите проблемный плагин
- Обновите или удалите проблемный плагин

---

## 📋 Чек-лист

- [ ] Проверен `error_log` в корне сайта (не `debug.log`)
- [ ] Добавлено принудительное логирование PHP в начало `wp-config.php`
- [ ] Отключены плагины через базу данных
- [ ] Проверена папка `mu-plugins`
- [ ] Использован тестовый скрипт `test-load.php`
- [ ] Проверены логи Apache

---

## 🆘 Если ничего не помогает

1. **Сохраните все логи** (error_log, Apache logs)
2. **Обратитесь к хостинг-провайдеру** с логами
3. **Проверьте использование ресурсов** (память, CPU, диск)

---

## 📝 Полезные пути для вашего сервера

- Корень сайта: `/home/s1149026/gustolocal.es/`
- error_log: `/home/s1149026/gustolocal.es/error_log`
- debug.log: `/home/s1149026/gustolocal.es/wp-content/debug.log`
- Плагины: `/home/s1149026/gustolocal.es/wp-content/plugins/`
- Темы: `/home/s1149026/gustolocal.es/wp-content/themes/`
- wp-config.php: `/home/s1149026/gustolocal.es/wp-config.php`




