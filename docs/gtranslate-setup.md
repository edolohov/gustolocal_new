# Настройка GTranslate плагина

## ✅ Что было сделано

Добавлены кастомные CSS стили для красивого отображения виджета GTranslate с флажками переключения языка.

### Файл: `twentytwentyfour/functions.php`
Функция: `gtranslate_custom_styling()` (строка 1843)

## 🎨 Стилизация

### Основные особенности:

1. **Позиционирование:**
   - Фиксированное положение в верхнем левом углу
   - `top: 20px`, `left: 20px` на десктопе
   - Адаптивное позиционирование на мобильных устройствах

2. **Внешний вид:**
   - Полупрозрачный белый фон с эффектом blur
   - Скругленные углы (12px)
   - Тень для глубины
   - Плавные анимации при наведении

3. **Флажки:**
   - Размер: 28x20px на десктопе, 24x18px на планшете, 22x16px на мобильном
   - Скругленные углы
   - Эффект увеличения при наведении (scale 1.15)
   - Активный флаг выделен синей рамкой

4. **Адаптивность:**
   - Автоматическая адаптация под разные размеры экрана
   - На мобильных устройствах виджет сдвигается ниже, чтобы не перекрывать меню

## ⚙️ Настройка GTranslate плагина

### Рекомендуемые настройки:

1. **Widget look:** `Flags` (флажки)
2. **Translate from:** `Russian` (русский)
3. **Show floating language selector:** ✅ Включено
4. **Position:** `Top left` (верхний левый угол)
5. **Flag size:** `24px` или `28px`
6. **Flag style:** `2D (avg)` или `3D`
7. **Flag languages:** Выбрать только нужные:
   - ✅ Russian
   - ✅ Spanish  
   - ✅ English

### Дополнительные настройки (опционально):

- **Show in menu:** ❌ Отключено (используем floating widget)
- **Auto-switch to browser language:** ❌ Отключено (у нас своя логика)
- **Native language names:** ✅ Можно включить для лучшего UX

## 📱 Адаптивность

### Десктоп (> 768px):
- Позиция: `top: 20px, left: 20px`
- Размер флажков: `28x20px`
- Padding: `8px`

### Планшет (≤ 768px):
- Позиция: `top: 15px, left: 15px`
- Размер флажков: `24x18px`
- Padding: `6px`

### Мобильный (≤ 480px):
- Позиция: `top: 10px, left: 10px`
- Размер флажков: `22x16px`
- Padding: `5px`

### Мобильный с меню (≤ 1024px):
- Позиция: `top: 70px` (ниже мобильного меню)

## 🎯 Z-index и приоритеты

- **GTranslate виджет:** `z-index: 9998`
- **Модалки:** `z-index: 9999+` (выше виджета)
- **Меню:** `z-index: 1000+`

Виджет не будет перекрывать модалки, но будет виден поверх обычного контента.

## 🔧 Кастомизация

Если нужно изменить позицию или стиль:

1. Откройте `twentytwentyfour/functions.php`
2. Найдите функцию `gtranslate_custom_styling()` (строка 1843)
3. Измените CSS стили по необходимости

### Примеры изменений:

**Изменить позицию на правый верхний угол:**
```css
.gt_float_switcher,
.gtranslate_wrapper {
    left: auto !important;
    right: 20px !important;
}
```

**Изменить размер флажков:**
```css
.gt_float_switcher img,
.gtranslate_wrapper img {
    width: 32px !important;
    height: 24px !important;
}
```

**Изменить цвет активного флага:**
```css
.gt_float_switcher img.gt_current,
.gtranslate_wrapper img.gt_current {
    border: 2px solid #your-color !important;
}
```

## ⚠️ Важные замечания

1. **Не конфликтует с кастомной системой переводов:**
   - GTranslate работает параллельно с нашей системой
   - Пользователь может использовать оба варианта

2. **SEO:**
   - GTranslate использует JavaScript-перевод (не SEO-friendly)
   - Это временное решение до создания полноценных страниц `-es` и `-en`

3. **Производительность:**
   - Виджет загружается асинхронно
   - Не блокирует загрузку страницы

## 📝 Следующие шаги

1. ✅ Настроить GTranslate плагин в админке WordPress
2. ✅ Включить floating widget в настройках
3. ✅ Выбрать только нужные языки (RU, ES, EN)
4. ✅ Протестировать на разных устройствах
5. ⚠️ После создания страниц `-es` и `-en` можно отключить GTranslate

## 🐛 Решение проблем

### Виджет не отображается:
- Проверьте что GTranslate активирован
- Проверьте настройки "Show floating language selector"
- Очистите кэш браузера

### Виджет перекрывает контент:
- Проверьте z-index в настройках темы
- Убедитесь что нет конфликтов с другими плагинами

### Стили не применяются:
- Проверьте что CSS загружается (View Source → найти стили)
- Убедитесь что нет конфликтов с другими стилями
- Попробуйте увеличить специфичность селекторов

