<?php
/**
 * ИСПРАВЛЕННЫЙ ФРАГМЕНТ ДЛЯ wp-config.php
 * 
 * ПРОБЛЕМА: Константы WP_MEMORY_LIMIT и WP_MAX_MEMORY_LIMIT были определены ДВАЖДЫ
 * РЕШЕНИЕ: Определить их только ОДИН РАЗ с правильными значениями
 * 
 * ИНСТРУКЦИЯ:
 * 1. Найдите в вашем wp-config.php первое определение памяти (обычно строка ~73)
 * 2. Замените его на код ниже
 * 3. Удалите дублирующие define() из секции Wordfence
 */

// ============================================
// НАСТРОЙКИ ПАМЯТИ (ИСПРАВЛЕНО - БЕЗ ДУБЛИРОВАНИЯ)
// ============================================
// ВАЖНО: Определяем только ОДИН РАЗ с правильными значениями!
define('WP_MEMORY_LIMIT', '512M');        // Было 256M - УВЕЛИЧЕНО
define('WP_MAX_MEMORY_LIMIT', '1024M');   // Было 512M - УВЕЛИЧЕНО
@ini_set('memory_limit', '1024M');        // Устанавливаем лимит PHP

// ============================================
// НАСТРОЙКИ ДЛЯ WORDFENCE СКАНИРОВАНИЯ
// ============================================
// Увеличиваем лимит времени выполнения до 10 минут (600 секунд)
@ini_set('max_execution_time', 600);
@set_time_limit(600);

// Увеличиваем таймаут для сетевых операций
@ini_set('default_socket_timeout', 600);

// Убеждаемся, что WP Cron включен (нужен для фоновых задач Wordfence)
define('DISABLE_WP_CRON', false);

/* That's all, stop editing! Happy publishing. */




