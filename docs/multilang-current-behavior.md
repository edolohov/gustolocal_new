# Текущее поведение мультиязычности: Что видит пользователь

## 🔍 Что происходит сейчас при визите пользователя

### Сценарий 1: Первый визит (нет cookie, нет localStorage)

**Человек с испанским IP заходит на сайт:**

1. **Серверная часть (PHP):**
   - ❌ НЕТ проверки IP адреса
   - ❌ НЕТ проверки Accept-Language заголовка
   - ✅ Показывает русскую версию по умолчанию (`get_current_language()` возвращает 'ru')
   - ✅ Страница загружается на русском языке

2. **Клиентская часть (JavaScript):**
   - ✅ Проверяет `localStorage.getItem('user_language')` - пусто
   - ✅ Вызывает `getBrowserLanguage()`:
     ```javascript
     var lang = navigator.language || navigator.userLanguage;
     // Например: "es-ES", "es", "en-US", "en"
     ```
   - ✅ Если язык начинается с `es` → редирект на `/es/`
   - ✅ Если язык начинается с `en` → редирект на `/en/`
   - ✅ Если язык русский или другой → остается на `/` (русский)

**Итог:**
- ✅ **Если браузер на испанском** → автоматический редирект на `/es/` (испанская версия)
- ❌ **Если браузер на русском** → остается на `/` (русская версия), **НЕЗАВИСИМО от IP**

### Сценарий 2: Повторный визит (есть cookie/localStorage)

**Человек с испанским IP, но уже был на сайте:**

1. **Серверная часть (PHP):**
   - ✅ Проверяет cookie `user_language`
   - ✅ Если cookie = 'es' и текущий URL не `/es/` → редирект на `/es/`
   - ✅ Если cookie = 'en' и текущий URL не `/en/` → редирект на `/en/`

2. **Клиентская часть (JavaScript):**
   - ✅ Проверяет `localStorage.getItem('user_language')`
   - ✅ Если сохраненный язык не совпадает с URL → редирект

**Итог:**
- ✅ Пользователь видит язык, который он выбирал ранее (сохранен в cookie/localStorage)

## 📊 Таблица: Что видит пользователь

| IP адрес | Язык браузера | Cookie | Что увидит |
|----------|---------------|--------|------------|
| Испания | es-ES | Нет | ✅ `/es/` (испанский) |
| Испания | ru-RU | Нет | ❌ `/` (русский) |
| Испания | en-US | Нет | ✅ `/en/` (английский) |
| Испания | es-ES | es | ✅ `/es/` (испанский) |
| Испания | ru-RU | es | ✅ `/es/` (испанский) - по cookie |
| Россия | es-ES | Нет | ✅ `/es/` (испанский) - по браузеру |
| Россия | ru-RU | Нет | ✅ `/` (русский) |

## ⚠️ Важные моменты

### 1. IP адрес НЕ используется
- ❌ Нет определения страны по IP
- ❌ Нет геолокации
- ✅ Используется только язык браузера (`navigator.language`)

### 2. Определение языка происходит на клиенте
- ✅ JavaScript выполняется ПОСЛЕ загрузки страницы
- ⚠️ Пользователь сначала видит русскую версию, потом происходит редирект
- ⚠️ Это может быть заметно (мигание страницы)

### 3. Приоритет определения языка

**На сервере (PHP):**
1. Cookie `user_language` (если есть)
2. По умолчанию: русский

**На клиенте (JavaScript):**
1. localStorage `user_language` (если есть)
2. Язык браузера (`navigator.language`)
3. По умолчанию: русский

## 🎯 Примеры реальных сценариев

### Пример 1: Испанец с испанским браузером
```
1. Заходит на example.com
2. Сервер показывает русскую версию
3. JavaScript определяет: navigator.language = "es-ES"
4. JavaScript редиректит на example.com/es/
5. Пользователь видит испанскую версию
```

### Пример 2: Испанец с русским браузером
```
1. Заходит на example.com
2. Сервер показывает русскую версию
3. JavaScript определяет: navigator.language = "ru-RU"
4. JavaScript НЕ редиректит (остается на /)
5. Пользователь видит русскую версию ❌
```

### Пример 3: Испанец, который уже был на сайте
```
1. Заходит на example.com
2. Сервер проверяет cookie: user_language = "es"
3. Сервер редиректит на example.com/es/
4. Пользователь сразу видит испанскую версию ✅
```

## 🔧 Что можно улучшить

### 1. Определение по IP (геолокация)
```php
// Можно добавить проверку IP через сервис типа:
// - MaxMind GeoIP
// - Cloudflare (если используется)
// - IPStack
// - GeoIP2
```

**Плюсы:**
- ✅ Работает до загрузки страницы
- ✅ Не зависит от настроек браузера

**Минусы:**
- ❌ Требует внешний API или базу данных
- ❌ Может быть неточным (VPN, прокси)
- ❌ Дополнительные запросы

### 2. Серверное определение по Accept-Language
```php
// Проверка заголовка HTTP_ACCEPT_LANGUAGE на сервере
// Редирект происходит ДО загрузки страницы
```

**Плюсы:**
- ✅ Работает до загрузки страницы
- ✅ Нет мигания контента
- ✅ Лучше для SEO

**Минусы:**
- ❌ Пользователь может изменить настройки браузера

### 3. Комбинированный подход
```
1. Сервер: Проверка Accept-Language → редирект
2. Клиент: Проверка navigator.language → редирект (fallback)
3. Cookie: Сохранение выбора пользователя
```

## 📝 Текущий код

### Серверная часть (PHP)
```php
// functions.php, строка 647
function simple_language_redirect() {
    // Работает только если есть cookie
    if (isset($_COOKIE['user_language'])) {
        // Редирект на основе cookie
    }
}
```

### Клиентская часть (JavaScript)
```javascript
// functions.php, строка 600
function getBrowserLanguage() {
    var lang = navigator.language || navigator.userLanguage;
    if (lang.startsWith('es')) return 'es';
    if (lang.startsWith('en')) return 'en';
    return 'ru';
}
```

## ✅ Вывод

**Сейчас:**
- ✅ Испанец с испанским браузером → увидит испанскую версию
- ❌ Испанец с русским браузером → увидит русскую версию
- ✅ Испанец, который уже был на сайте → увидит сохраненный язык

**Рекомендация:**
- Добавить серверное определение по Accept-Language для лучшего UX
- IP-геолокация опциональна (может быть неточной)

