# 🚨 Решение проблемы Wordfence на ЗАРАЖЕННОМ сайте

## Проблема
Сканирование Wordfence зависает через 5 минут на зараженном сайте, несмотря на настройки.

---

## ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА В wp-config.php

**Вы определили константы памяти ДВАЖДЫ!** В PHP нельзя переопределить константу через `define()`, поэтому первое определение имеет приоритет.

### ❌ Текущая ситуация (НЕПРАВИЛЬНО):
```php
// Первое определение (строка ~73)
define('WP_MEMORY_LIMIT', '256M');      // ← Это имеет приоритет!
define('WP_MAX_MEMORY_LIMIT', '512M');  // ← Это имеет приоритет!

// Позже вы пытаетесь переопределить (строка ~80)
define('WP_MEMORY_LIMIT', '512M');      // ← НЕ РАБОТАЕТ!
define('WP_MAX_MEMORY_LIMIT', '1024M'); // ← НЕ РАБОТАЕТ!
```

**Результат:** WordPress использует только 256M/512M памяти, а не 512M/1024M!

### ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ:

**Найдите в wp-config.php эти строки:**
```php
define( 'WP_MEMORY_LIMIT', '256M' );
define( 'WP_MAX_MEMORY_LIMIT', '512M' );
```

**Замените их на:**
```php
define( 'WP_MEMORY_LIMIT', '512M' );
define( 'WP_MAX_MEMORY_LIMIT', '1024M' );
```

**И удалите дублирующие строки из секции Wordfence**, оставив только:
```php
// ============================================
// НАСТРОЙКИ ДЛЯ WORDFENCE СКАНИРОВАНИЯ
// ============================================
@ini_set('max_execution_time', 600);
@set_time_limit(600);
@ini_set('memory_limit', '1024M');
```

---

## ✅ ИСПРАВЛЕНИЕ НАСТРОЕК WORDFENCE

### Проблема: 15 секунд на этап - слишком мало!

В настройках Wordfence (Wordfence → Scan → Manage Scan):

1. **"Максимальное время для каждого этапа сканирования"** (Maximum time for each scan stage)
   - **Текущее значение:** 15 секунд ❌
   - **Установите:** 60-120 секунд ✅
   - **Рекомендация:** Начните с 60, если все равно зависает - увеличьте до 120

2. **"Ограничение по времени, в течение которого сканирование может выполняться"** (Time limit during which scanning can run)
   - **Текущее значение:** пусто (по умолчанию 3 часа)
   - **Оставьте пустым** или установите 10800 (3 часа)

3. **"Какой объем памяти должен запрашивать Wordfence при сканировании"** (How much memory should Wordfence request when scanning)
   - **Текущее значение:** 256
   - **Установите:** 512 или 1024

4. **"Использовать сканирование с низким уровнем ресурсов"** ✅
   - **Убедитесь, что ВКЛЮЧЕНО** (галочка стоит)

---

## 🎯 ПОШАГОВЫЙ ПЛАН ДЕЙСТВИЙ

### Шаг 1: Исправить wp-config.php (КРИТИЧНО!)

1. Откройте `wp-config.php` через SFTP
2. Найдите первое определение памяти (обычно строка ~73):
   ```php
   define( 'WP_MEMORY_LIMIT', '256M' );
   define( 'WP_MAX_MEMORY_LIMIT', '512M' );
   ```
3. Замените на:
   ```php
   define( 'WP_MEMORY_LIMIT', '512M' );
   define( 'WP_MAX_MEMORY_LIMIT', '1024M' );
   ```
4. Найдите секцию Wordfence и удалите дублирующие `define()` строки
5. Сохраните файл

### Шаг 2: Обновить настройки Wordfence

1. Wordfence → Scan → Manage Scan
2. Установите "Максимальное время для каждого этапа" = **60** (или 120)
3. Установите "Какой объем памяти должен запрашивать Wordfence" = **512** (или 1024)
4. Убедитесь, что "Использовать сканирование с низким уровнем ресурсов" **ВКЛЮЧЕНО**
5. Сохраните изменения

### Шаг 3: Очистить сайт от вредоносного ПО ПЕРЕД сканированием

**Вредоносный код может блокировать сканирование!** Сначала удалите зараженные файлы:

#### 3.1. Используйте скрипт поиска бэкдоров

1. Загрузите `docs/find-backdoor-script.php` в корень сайта
2. Откройте: `https://gustolocal.es/find-backdoor-script.php?key=YOUR_PASSWORD`
3. Скрипт найдет все вредоносные файлы

#### 3.2. Удалите известные вредоносные файлы

Согласно вашему анализу (`docs/cleanup-gustolocal-analysis.md`), удалите:

**Безопасно удалить сразу (29 файлов):**
- Все файлы `postnews.php` (4 файла)
- `eDE9CW.php` (1 файл)
- Все файлы в `cgi-bin/` (4 файла)
- Все файлы в `images/images/` (11 файлов)
- Все файлы в `wp-admin/css/css/` (3 файла)
- Подозрительные файлы в `wp-includes/` (6 файлов)

**Через SFTP:**
```bash
# Удалите эти файлы/папки:
/home/s1149026/gustolocal.es/postnews.php
/home/s1149026/gustolocal.es/wp-admin/postnews.php
/home/s1149026/gustolocal.es/wp-content/postnews.php
/home/s1149026/gustolocal.es/wp-admin/eDE9CW.php
/home/s1149026/gustolocal.es/cgi-bin/ (всю папку)
/home/s1149026/gustolocal.es/images/images/ (всю папку)
/home/s1149026/gustolocal.es/wp-admin/css/css/ (всю папку)
```

#### 3.3. Проверьте cron задачи

Вредоносный код может воссоздавать файлы через cron:

1. Войдите в панель хостинга
2. Найдите раздел "Cron задачи" или "Планировщик задач"
3. Удалите все подозрительные задачи (особенно те, что содержат `cgi-bin`, `postnews.php`, `cache.php`)

### Шаг 4: Попробуйте сканирование снова

После очистки:
1. Подождите 5-10 минут
2. Запустите сканирование Wordfence
3. Если все еще зависает - переходите к Шагу 5

---

## 🔄 АЛЬТЕРНАТИВНЫЕ МЕТОДЫ СКАНИРОВАНИЯ

Если Wordfence все еще не работает, используйте альтернативы:

### Вариант 1: Sucuri Security (бесплатный)

1. Установите плагин **Sucuri Security**
2. Запустите сканирование (обычно работает быстрее на зараженных сайтах)
3. Используйте для первичной очистки

### Вариант 2: MalCare Security (бесплатный)

1. Установите плагин **MalCare Security**
2. Сканирование происходит на их серверах (не нагружает ваш сайт)
3. Хорошо работает на зараженных сайтах

### Вариант 3: Ручное сканирование через скрипт

Используйте ваш скрипт `find-backdoor-script.php`:
1. Он найдет все подозрительные файлы
2. Покажет содержимое файлов
3. Поможет найти активные бэкдоры

### Вариант 4: Онлайн-сканеры

1. **Sucuri SiteCheck**: https://sitecheck.sucuri.net/
2. **Quttera**: https://quttera.com/
3. Введите ваш URL и получите отчет

---

## 🛠️ ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ ДЛЯ ЗАРАЖЕННОГО САЙТА

### 1. Увеличьте лимиты еще больше

В `wp-config.php` добавьте (если хостинг позволяет):

```php
// Экстремальные настройки для зараженного сайта
@ini_set('max_execution_time', 1800); // 30 минут
@set_time_limit(1800);
@ini_set('memory_limit', '2048M');
@ini_set('default_socket_timeout', 600);
```

### 2. Отключите другие плагины временно

Вредоносный код может быть в других плагинах:
1. Переименуйте папку `wp-content/plugins` в `wp-content/plugins-disabled`
2. Попробуйте сканирование
3. Если работает - включайте плагины по одному

### 3. Используйте режим обслуживания

Включите режим обслуживания WordPress во время сканирования:
1. Установите плагин **WP Maintenance Mode**
2. Включите режим обслуживания
3. Запустите сканирование (меньше нагрузка на сервер)

---

## 📋 ЧЕК-ЛИСТ ИСПРАВЛЕНИЯ

- [ ] Исправлен `wp-config.php` - удалено дублирование констант памяти
- [ ] Установлено `WP_MEMORY_LIMIT` = 512M (в первом определении)
- [ ] Установлено `WP_MAX_MEMORY_LIMIT` = 1024M (в первом определении)
- [ ] Удалены дублирующие `define()` из секции Wordfence
- [ ] В Wordfence установлено время этапа = 60-120 секунд
- [ ] В Wordfence установлена память = 512-1024 MB
- [ ] Включен режим низкого потребления ресурсов в Wordfence
- [ ] Удалены все известные вредоносные файлы (29 файлов)
- [ ] Проверены и очищены cron задачи
- [ ] Запущено сканирование Wordfence
- [ ] Если не работает - установлен альтернативный сканер (Sucuri/MalCare)

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Вредоносный код может блокировать сканирование** - сначала удалите зараженные файлы
2. **Константы PHP нельзя переопределить** - исправьте первое определение в wp-config.php
3. **15 секунд на этап слишком мало** для зараженного сайта - установите минимум 60 секунд
4. **Проверьте cron задачи** - они могут воссоздавать удаленные файлы
5. **Смените все пароли** после очистки сайта

---

## 🔗 Полезные ссылки

- [Официальная документация Wordfence по устранению неполадок](https://www.wordfence.com/help/scan/troubleshooting/)
- Скрипт поиска бэкдоров: `docs/find-backdoor-script.php`
- Анализ зараженных файлов: `docs/cleanup-gustolocal-analysis.md`
- Руководство по удалению бэкдоров: `docs/backdoor-removal-guide.md`




